# UI/UX 重構 — 任務追蹤表

> 依 `UIUX_SPEC.md` 拆解。負責 agent 每完成一項**立即更新本表狀態欄**（並同步 Cowork 任務清單）。
> 狀態：⬜ 未開始 · 🔄 進行中 · ✅ 完成 · ⏸ 卡住（於「備註」寫原因）
> 鐵則：**每階段獨立可驗證、不得破壞現有功能**；後端改動 `py_compile`；前端改動檢查 HTML id ↔ JS 參照一致；上游檔（spider/stream/room/utils/ab_sign/proxy/logger/initializer）**不碰**。

---

## 進度總覽

| 階段 | 名稱 | 狀態 | 完成度 |
|------|------|------|--------|
| 0 | 骨架（tokens + 元件 + Alpine + 路由殼） | ✅ | 4/4 |
| 1 | 遷移現有頁到新框架 | ✅ | 5/5 |
| 2 | 儀表板 panel 化 + 全域狀態 | ✅ | 4/4 |
| 3 | 回放同步彈幕 | ✅ | 3/3 |
| 4 | 新頁（P1-1 錄製設定 / P1-2 統計）+ 壓縮占比 | ✅ | 4/4 |
| 5 | 技術債清理 | ✅ | 4/4 |
| 6 | 破版修復：全域 CSS 洩漏 + 統計頁載入失敗 | ✅ | 2/2 |
| 7 | 側邊欄排版重寫（icon/label 分離） | ✅ | 1/1 |
| 8 | 回放頁改「先選主播、再選日期」 | ✅ | 1/1 |
| 9 | 回放頁清單加篩選/排序/可調欄寬 + 放不出影片的診斷欄位 | ✅ | 2/2 |
| 10 | 修復根因：分段錄製 file_path 存成 %03d 樣板，回放全部空清單 | ✅ | 2/2 |
| 11 | 回放頁：主播/日期各自獨立篩選 + 彈幕欄位 + 排版重排 + 主播分組修正 | ✅ | 4/4 |
| 12 | 回放支援分段錄製多檔案播放清單 | ✅ | 3/3 |
| 13 | 彈幕收集診斷 + 修復 _stop 命名衝突 bug（協調器整輪 crash） | ✅ | 3/3 |
| 14 | 彈幕轉字幕 UX 改版（主播→日期→片段，跟回放頁一致）+ 回放頁一鍵產字幕按鈕 | ✅ | 2/2 |
| 15 | 統計頁重新規劃：修主播分組 bug + 表格改 mtInit + 加入彈幕統計 | ✅ | 3/3 |

---

## 階段 0 — 骨架（不動功能）

| # | 任務 | 檔案 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|------|
| 0.1 | 建 design tokens CSS（§4 色/間距/圓角/字級/陰影） | web_static/index.html（`<style>` 內新增區塊） | tokens 全定義、無硬編色 | ✅ | token 全部 scope 在 `#new-shell{ }` 選擇器內（而非全域 `:root`），刻意不覆蓋舊頁原本就有的 `--bg/--border/--fg/--accent` 等同名變數（新舊定義的色碼不同，若寫進全域 `:root` 會讓現有 4 分頁的底色/邊框/文字色跟著變，違反「不得破壞現有功能」）。新元件在 `#new-shell` 內用新 token；階段1把舊頁內容搬進 `#new-shell` 後這批 token 自然全域生效，屆時再考慮要不要把舊 `:root` 也换成新色碼。 |
| 0.2 | 建元件 class（§5：card/table/form/button/badge/switch/toast/modal/空狀態/骨架） | web_static/index.html | 各元件可獨立套用 | ✅ | 全部用 `c-` 前綴（`.c-panel/.c-card/.c-table/.c-btn(.c-btn-primary/secondary/danger)/.c-badge(.is-live/is-cooldown/is-off/is-danger/is-info)/.c-switch/.c-toast/.c-modal/.c-modal-mask/.c-empty/.c-skeleton/.c-dot`），且每條規則都以 `#new-shell .c-xxx` 限定作用範圍，跟舊版 `.card/.btn/.pill/.switch/.modal/.toast/.empty` 完全分流、零覆蓋風險。skeleton 用 CSS gradient shimmer 動畫（`@keyframes c-skeleton-shine`）。 |
| 0.3 | 放 Alpine.js 本地一份進 web_static/（無 CDN、離線可用） | web_static/alpine.min.js | 載入無 console error | ✅ | jsdelivr 在本 agent 的 Linux 沙箱 bash 被 proxy allowlist 擋（403 blocked-by-allowlist），改用 `mcp__workspace__web_fetch` 工具（走另一條網路路徑）從 `https://unpkg.com/alpinejs@3.14.3/dist/cdn.min.js` 抓到完整原始碼，`node -c` 驗證語法通過（44794 bytes）後存成本地檔。index.html 引入路徑用 `/static/alpine.min.js`（**不是**任務描述裡字面的相對路徑 `alpine.min.js`）——因為 `web_ui.py` 的 `index()` 是用 `send_from_directory` 直接在 `/` 吐 `index.html`（不是走 Flask 的 static route），若寫相對路徑瀏覽器會請求 `/alpine.min.js` 而 404；Flask app 建立時本來就有 `static_folder=web_static, static_url_path="/static"`（web_ui.py 864行），沿用這個既有掛載點即可、**完全沒有改 web_ui.py**。 |
| 0.4 | 側邊欄 + topbar + hash 路由空殼（§1/§2，5+ view 容器） | web_static/index.html | 點選單切 view、hash 變更、重整不掉頁 | ✅ | 策略＝「新殼與舊內容並存、預設關閉（feature flag）」：舊版 4 分頁（`<header>/<nav>/<main><section id="page-*">`）**完全沒有動一行**，新框架整包放在新增的 `#new-shell`（`position:fixed; inset:0; z-index:9000; display:none`），預設不顯示，使用者打開頁面看到的仍是 100% 原本的介面。進入方式：① 點 header 新增的「預覽新版介面 (Beta)」連結（`id="new-ui-entry"`，呼叫 `toggleNewUI()`）；② 直接帶新框架的 hash 開網址（如 `#streamers`）會在 `DOMContentLoaded` 自動開殼。新殼內是 `x-data="shellApp()"` 的 Alpine 元件：側邊欄依 §1 選單樹分監控/內容/設定三組共 10 個連結（`href="#dashboard"` 等原生錨點，不攔截 click），10 個 `.view` 容器（`id="view-dashboard" / view-streamers / view-danmaku / view-player / view-stats / view-settings-recording / view-settings-notify / view-settings-compress / view-settings-disk / view-settings-system`，超過規格要求的 5+）用 `:class="{'is-active': route==='...'}"` 切換顯示，`window.addEventListener('hashchange', ...)` 驅動 `route`；topbar 有麵包屑（`x-text="breadcrumb"`）+ 健康燈/port/刷新的**純佔位**（無 API 呼叫，階段2才接資料）+「◀ 返回舊版」；<900px 有漢堡鈕收合側邊欄成抽屜。**驗收方式**：改用瀏覽器實測前用靜態方式驗證——`html.parser` 逐標籤配對檢查（0 錯誤、EOF 時 stack 清空）、`id="..."` 全域唯一性 grep（0 重複）、CSS `{`/`}` 計數配對（164/164）、`onclick=/@click=` 呼叫的函式名比對 `function xxx()/window.xxx=function` 定義清單（0 個缺定義，僅 2 個 regex 誤報：`onclick="if(...)"` 的舊碼與 `@click="sidebarOpen=..."` 賦值運算式，皆非函式呼叫）。因為 stage0 是純骨架（10 個 view 目前都只放 `.c-empty` 佔位文字 + 「請用返回舊版」提示），**沒有實機瀏覽器可用時仍能高信心確認不會壞舊頁**：新增的一切都在 `#new-shell` 這個獨立子樹 + 獨立 script block（函式名 `shellApp/toggleNewUI/SHELL_ROUTES/SHELL_LABELS` 與既有腳本零命名衝突）裡，舊 DOM/CSS/JS 一個字元都沒改（除了 header 內新增一顆入口連結、`</head>`前一行 `<script defer>`、`</main>`後插入新 `<div id="new-shell">`區塊、`</script>`後插入新 `<script>` 區塊，四處都是「新增」而非「修改」）。**給階段1接手**：把舊 `<section id="page-dashboard">` 等內容逐步搬進對應 `#view-dashboard` 等容器時，記得同步把行內樣式/舊 class 換成這裡定義的 `c-*` class + `var(--token)`，並移除對應 `.c-empty` 佔位區塊；`showTab(name)` 之類舊函式屆時要嘛保留給「返回舊版」相容用，要嘛評估整個拿掉改成純 hash 路由（決策留給階段1）。 |

## 階段 1 — 遷移現有頁（等價替換，先不加新功能）

**策略決定（本階段最大風險點：id 衝突）**：捨棄「新舊殼並存、二選一顯示」的做法，改採**單一 DOM、單一綁定**——把舊版 `<header>/<nav>/<main><section id="page-*">` 的實際內容（連同其中每一個 `id`）**整段搬移**進對應的 `#view-*` 容器，而不是複製一份。舊版 `<header>`、`<nav>`、`<main>` 外殼本身連同 4 個 `<section id="page-*">` 包裹層已整個移除；因為是「搬」不是「複製」，全站每個 `id` 永遠只存在一份，結構上不可能撞 id，比「兩份 DOM +顯示切換」更穩。新殼（`#new-shell`）現在是唯一介面，載入即自動開啟（`toggleNewUI()`／「預覽新版介面」／「返回舊版」等雙版切換機制已移除，沒有東西可切了）。JS 函式簽章、`onclick`/`getElementById` 目標 id 100% 不變，只是 DOM 位置換了；已用腳本驗證：0 重複 id、HTML 標籤 100% 配對、CSS `{`/`}` 164/164 配對、42 個 onclick/@click 呼叫全部有對應函式定義、85 個 `getElementById` 全部能在 DOM 中找到目標。`#modal-mask`／`#toast` 兩個 `position:fixed` 元件原本是 `#new-shell` 的外部同層兄弟節點，會被 `#new-shell`（`z-index:9000`）疊住看不見——已一併搬進 `#new-shell` 內（作為 `.shell-layout` 之後的兄弟節點）解決疊層問題。舊版視覺 class（`.card/.pill/.btn/.switch/.modal/.toast/.empty/table` 等，定義在檔案最上面全域 `<style>`，非 `#new-shell` scope）沿用不變，繼續生效；`c-panel/c-card` 等新元件只套用在本階段新寫的外層容器（panel 標題列、settings 分頁排版），表格/徽章/按鈕等既有 JS 動態產生的 HTML 片段本階段**維持原 class**（未逐一換成 `c-*`），理由：那些字串是在 `renderStreamers()/refreshDashboard()` 等函式內用樣板字串組出來的，逐一改字串風險/工作量遠高於「先等價搬移」的階段目標，視覺統一留給後續打磨階段。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 1.1 | 儀表板搬進新框架（先等價，panel 化留階段 2） | KPI/正在錄製/健康膠囊功能不變 | ✅ | disk-banner/health-box/KPI cards/active-tbody/cooldown-section 原樣搬進 `#view-dashboard`，外層包 `c-panel`。原 header 的 `updated-at`/`auto-status`/「↻ 立即檢查開播」(`recheckLive()`) 移到健康狀態 panel 標題列（先放這裡，panel 化精修留階段2）。「⚙ 通知設定」收合入口已移除（設定改走側邊欄常駐子頁，見1.5）。 |
| 1.2 | 主播管理搬遷（改名、批量收下拉、套新表格/徽章） | 新增/編輯/批量/toggle/特別關注 全正常 | ✅ | 標題已改「主播管理」。批量操作收進「批量 ▾」下拉（新增 `toggleBulkMenu()`，純顯示/隱藏，不動 `batchAction/toggleSelectAll/clearSelection` 既有邏輯），下拉在選取項目時才顯示（沿用原 `bulk-actions`/`normal-actions` 顯隱邏輯）、點外部自動關閉。表格/徽章視覺沿用既有 class（見上方策略說明）。 |
| 1.3 | 彈幕頁搬遷（套新元件，轉字幕移頁首 panel） | 查詢/擷取開關/匯出/轉字幕 全正常 | ✅ | 彈幕轉字幕本來就在頁首，維持原位置（符合規格「移到頁首 panel」，本來就是）。整段包進 `c-panel`。 |
| 1.4 | 回放頁搬遷（雙欄留階段 3，先等價） | 日期/清單/播放/自動下一段/字幕 全正常 | ✅ | 三欄（日期/播放器/當天清單）flex 佈局原樣搬進 `c-panel`，未改雙欄同步彈幕（階段3範圍）。 |
| 1.5 | 「⚙通知設定」彈出面板拆進 #settings/{notify,compress,disk,system} 四子頁 | 四子頁讀寫/測試 全等價現有 | ✅ | 拆法：`#settings/notify`＝健康警報(al-*)＋Discord錄製通知(rn-*)兩個 panel，沿用共用的 `saveAlerts()/testAlerts()`；`#settings/compress`＝cp-* 全部欄位+進度條，`scanCompress()` 本來就會先存設定再掃描，等價原行為；`#settings/disk`＝dk-* 欄位，新增 `saveDiskPolicy()`（只存不清理，補一顆獨立「儲存」鈕，`previewCleanup()/runCleanup()` 本來就會先存）；`#settings/system`＝版本/更新(bar-applied-sha/bar-update-info) + 「目前設定摘要」(bar-savepath/bar-savepath-status/bar-folder-rules/bar-root)，port/log/關於留階段2/4。舊版「開合面板才 lazy-load」機制（`toggleAlertsPanel()`/`ALERTS_LOADED` 判斷）已移除，改成頁面載入時 `loadAlerts()` 直接 eager-load 一次（不管先進哪個子頁欄位都有資料），`refreshCompressStatus()` 的 `alerts-panel` 顯示判斷guard 一併移除（背景持續刷新，成本很低）。 |

**JS 路由 dispatch 調整**：舊版 `showTab(name)` 依賴 `<nav button>`/`<section class="active">` 已不存在，改寫成 `onShellRouteChange(route)`（保留原本「切到哪一頁該刷新什麼資料」的 dispatch 邏輯：streamers→`refreshStreamers()`、danmaku→`initDanmaku()`、player→`initPlayer()`、dashboard→`refreshDashboard()`），由 `shellApp.syncRoute()` 在每次 hash 改變時呼叫。儀表板 5 秒輪詢的啟用條件從 `document.getElementById('page-dashboard').classList.contains('active')` 改成判斷目前 hash 是否為 `dashboard`（含空 hash，等價於預設路由）。

## 階段 2 — 儀表板 panel 化 + 全域狀態

**策略決定**：全域輪詢**不新增第二個計時器**，而是把既有的「儀表板 5s 輪詢」（`refreshDashboard()`）升格成唯一的全域共用 poll——移除階段1加的「只有 hash===dashboard 才刷新」的 gate，改成無條件每 5s 執行一次（見檔尾 `setInterval(refreshDashboard, 5000)`）。理由：`#view-*` 容器在非目前路由時只是 CSS `display:none`，DOM 元素本身一直都在，`getElementById` 照樣抓得到、寫入照樣有效，所以 topbar/sidebar/dashboard 三處都能吃同一份 `/api/status` 回應，不必為 topbar/sidebar 另開一條輪詢，天然滿足「全域輪詢一次共用、避免重複打 API」。壓縮佇列 mini panel 同理：不新增輪詢，而是擴充既有 `_renderCompressBars()`（由 `#settings/compress` 頁本來就有的 `CP_TIMER`＝每 10s 打一次 `/api/compress`、在 `loadAlerts()` eager-load 時就啟動、與目前路由無關）多寫一份到儀表板的 `#dash-compress-panel`。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 2.1 | 儀表板多 panel 佈局（§3.1：KPI/健康/磁碟/正在錄製/壓縮佇列/近期通知） | 各 panel 有資料才顯示、KPI 可點跳頁 | ✅ | `#view-dashboard` 重排成：① KPI 列（`c-panel` 包 `.cards`，沿用原 7 張卡不砍成 6 張——比 §3.1 線框多留一張「主播總數」，判斷保留比拿掉更安全，不算功能倒退）② 系統健康／磁碟 並排兩個 `c-panel`（flex + `flex:1 1 320px`，窄螢幕自動疊列）③ 正在錄製 `c-panel`（表格/停止鈕/進度條原樣，只換外殼）④ cooldown 表格 ⑤ 壓縮佇列 `c-panel`（`id="dash-compress-panel"`，預設 `display:none`，只有 `active`=`queue.total` 或 `lanes.length` 真值時才顯示）。KPI 卡點擊：監測中/錄製中/主播總數 → `#streamers`；硬碟總用量/硬碟剩餘 → `#settings/disk`（用 `onclick="location.hash='...'"`，比 spec 線框只寫「錄製中」多補了幾張卡的跳轉，判斷「凡是能對應到一個設定/管理頁的 KPI 都應該可點」比只做一張更一致）。磁碟 panel 新增使用率 bar（`#dash-disk-bar`）+ 摘要文字（`#dash-disk-summary`：已用%／總量／剩餘／其中錄影占用），資料算法：`disk_used_bytes/disk_total_bytes`（磁碟機層級），另外用括號附註 `total_disk_bytes`（僅錄影資料夾占用，兩者語意不同，避免使用者誤讀）；spec 線框裡的「壓縮已省 120GB」這行**沒有做**——那需要 §6 列的 `/api/compress/overview`（階段4新後端），本階段不新增後端，資料不存在不硬湊。**「近期通知/活動」panel 整個沒做**：全站目前沒有任何事件記錄/歷史通知 API（`grep` 過 `web_ui.py` 全部 `/api/*` 路由，只有即時狀態,沒有 log/event 端點），硬做只能拿健康 issues 重複顯示一次（跟系統健康 panel 內容重疊、無新資訊），判斷『沒真資料就不做假 panel』比『為了湊 spec 六個區塊硬塞一個空殼』更安全，也符合 spec 自己寫的「panel 有資料才顯示」原則——這裡直接不建 DOM，等以後有 event-log 端點（建議放階段4/5）再補。 |
| 2.2 | 「↻ 立即檢查開播」按鈕（正在錄製 panel + 主播管理工具列）含確認/loading | 觸發 /api/recorder/restart，狀態態正確 | ✅ | 原本掛在階段1的健康 panel 標題列（`<a onclick="recheckLive()">`）搬到「正在錄製」panel 標題列（`c-panel-actions` 內的 `<button class="c-btn c-btn-sm recheck-live-btn">↻ 立即檢查開播</button>`），並在主播管理工具列（`#normal-actions`，清空自動恢復時間鈕左邊）加一顆同文案同 class 的（沿用該工具列既有的 `.btn.small` 視覺，不是 `c-btn`，因為緊鄰的 `清空自動恢復時間`/`+新增主播` 都是舊 class，混搭 `c-btn` 視覺會不一致）。兩顆鈕共用 `.recheck-live-btn` class 做 querySelectorAll 定位，`recheckLive()` 改寫：按下先 `confirm()`（文案改正——原文案寫「會中斷正在進行的錄製約 5 秒」，跟 spec §3.1 備註「進行中的錄製會續錄、不中斷已錄檔」矛盾，判斷這是舊文案不準確，已一併修正為 spec 的說法，屬於內容修正不是邏輯變更，風險低）；確認後兩顆鈕一起 `disabled=true` + 文字換成「重啟中…」（用 `dataset.origText` 存原文字，`finally` 還原），呼叫邏輯本身（`fetch('/api/recorder/restart', POST)`）完全沒動。 |
| 2.3 | topbar 全域健康燈 + 實際 port + 刷新（跨頁可見） | 切任意頁都看得到、hover 顯示問題 | ✅ | topbar 三個佔位換成真資料：健康燈 `#shell-health-dot`（`.c-dot` class 動態切 `ok/warn/danger`，`title` 屬性放 issues 摘要多行文字，hover 原生 tooltip 即可看到，不用額外做自訂 tooltip 元件）；port `#shell-port`，用 IIFE `initShellPort()` 在腳本解析當下直接讀 `location.port`（沒值則預設 8765）寫入（一次性、不需要輪詢，因為同一個頁面生命週期內 port 不會變）；刷新鈕 `onclick="refreshGlobalStatus(this)"`——按下時 `disabled` 該鈕、await `refreshDashboard()`（就是同一份全域 poll 函式，手動催一次 + toast「已重新整理」）、`finally` 恢復可點。資料來源：新函式 `updateShellChrome(d)`（定義在 `renderHealth()` 之後），從 `refreshDashboard()` 內既有的 `DASH_DATA`（`/api/status` 回應，內含 `health{status,issues[]}`）讀，**沒有另外呼叫 `/api/health`**——`/api/status` 的 `health` 欄位跟獨立的 `/api/health` 端點回傳同一份 `evaluate_current_health()` 結果（讀過 `web_ui.py` 861-953 行確認兩者呼叫同一函式），用其中一份即可，避免階段2要求的「別重複打 API」。 |
| 2.4 | sidebar 底常駐迷你狀態（錄製器/磁碟剩餘） | 全域輪詢一次共用、跨頁可見 | ✅ | sidebar 底部兩個佔位換真資料，一樣在 `updateShellChrome(d)` 裡跟 2.3 同一次寫入（同一份 `DASH_DATA`，零額外 API）：錄製器 `#shell-recorder-dot`／`#shell-recorder-text`——判斷邏輯是 `health.issues` 裡有沒有 `code === 'recorder_down'`（讀過 `src/health.py` 確認這是穩定 issue code 常數 `RECORDER_DOWN = "recorder_down"`），有則紅點+「（已停止）」文字，沒有則綠點；磁碟剩餘 `#shell-disk-free`——`disk_free_bytes / 1024**3` 轉 GB 顯示到小數1位，點的顏色三級：`disk_full` 真→紅、剩餘 < 20GB → 黃、其餘綠（20GB 門檻是本次判斷取的安全預設值，跟後端 `/api/disk/policy` 的 `pause_below_gb`/`resume_above_gb` 沒有掛勾，純粹前端顯示用；如果覺得該跟後端門檻同步，可在階段4/5 改成讀 `dk-pause`/`dk-resume` 欄位）。 |

## 階段 3 — 回放同步彈幕（§3.4）

**策略決定**：採 spec 建議的「輕量端點」路線（`GET /api/library/clip/<id>/danmaku`），沒有去動 `/api/library/day` 的 clip 物件加 `live_url`/`start_epoch`/`duration`——因為前端本來就已經有 `clip.id`（`playClip()`/`plClips` 既有欄位），輕量端點讓前端只需多打一支 API、不用自己在瀏覽器端重算時間對齊，符合任務說明「前端最單純」的建議。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 3.1 | 後端：clip 補 live_url/start_epoch/duration，或加 /api/library/clip/<id>/danmaku | 端點回該片段彈幕含影片內秒數偏移 | ✅ | `web_ui.py` 新增 `GET /api/library/clip/<clip_id>/danmaku`（放在既有 `/api/library/subtitle/<clip_id>` 之後）。**契約**：回應 `{clip_id, start_epoch, duration_sec, count, danmaku:[{id, ts, offset_sec, user, content}, ...]}`（依 `offset_sec` 升冪排序）；找不到 clip 回 404；模組未載入回 500。實作：重構既有 `_resolve_clip_id(clip_id)` → 新增 `_resolve_clip_session(clip_id)`（回傳 `{**session, "resolved_path": real}`，session 欄位含 `anchor_name/platform/live_url/start_time/end_time/duration_sec/file_path`，取自既有 `_finished_sessions_for_library()`），`_resolve_clip_id` 改成呼叫它取 `resolved_path` 的薄包裝、**呼叫端行為完全不變**（`/api/library/video`、`/api/library/subtitle` 兩個既有端點沒被動到）。新增純函式 `_clip_danmaku_offsets(rows, start_epoch, duration=None)` 算 `offset_sec = ts - start_epoch` 並丟掉範圍外的（`offset<0` 或 `offset > duration+5`，margin 沿用 `/api/danmaku/subtitle` 端點既有的 `+5` 慣例）——**沒有重寫時間對齊公式**，這正是 `danmaku_subtitle.py._offsets()` 已經在用的同一條規則，只是留原始 `id/ts` 給前端做二分搜尋/顯示（`_offsets()` 是為了組 ASS/SRT cue 才把欄位精簡掉，兩者職責不同所以沒有直接 import 共用，但公式一致）。查詢彈幕時優先用 `live_url`（`store.query(url=...)`）、沒有才 fallback 用 `anchor_name`——因為 `live_url` 是 `danmaku_capture.py` 寫入 `streamer_url` 時用的同一把房間網址，比主播名穩定（改名不受影響）。驗證：複製到 `/tmp` 後 `python3 -m py_compile` 通過；用 `ast` 抽出 `_clip_danmaku_offsets` 單獨在 Python 裡跑了 6 組斷言（正常過濾/邊界 margin/無 duration/空陣列/缺 ts 欄位不炸），全過。 |
| 3.2 | 右側彈幕欄：跟播捲動 + 目前行高亮 + 拖動進度跟跳 | timeupdate 驅動、二分搜尋定位 | ✅ | `web_static/index.html` 的 `#view-player` 三欄（日期/播放器/當天清單）後面加第 4 欄 `#pl-dm-col`（寬 300px、`border-left` 分隔，說明見下方「與線框的差異」）。JS：`PL_DM`（該 clip 完整彈幕，依 `offset_sec` 升冪，後端已排序）/`PL_DM_VIEW`（套用關鍵字搜尋後的顯示子集，無搜尋時等於 `PL_DM`）；`playClip(i)` 最後新增一行呼叫 `loadPlayerDanmaku(c.id)`（fetch 新端點，含 `PL_DM_LOAD_SEQ` race guard——連續快速切段時只套用最後一次回應，不會出現舊段彈幕蓋新段）。跟播：`<video>` 的 `timeupdate` 事件呼叫 `syncPlayerDanmaku(false)`、`seeked` 事件（使用者拖動/點擊進度條放開時觸發）呼叫 `syncPlayerDanmaku(true)` 強制重新捲動；核心用 `_bsearchDanmakuIndex(arr, t)` 對 `PL_DM_VIEW` 做二分搜尋找「`offset_sec <= t` 的最後一筆」，**不是線性掃描**（`timeupdate` 每秒觸發數次，資料量大時線性掃描會卡頓，二分搜尋 O(log n)）。目前行用 `.pl-dm-row.is-current` class 高亮（CSS 在 `<style>` 尾端新增，scope 在 `#new-shell`）+ `scrollIntoView({block:'center'})` 自動捲動。用 Node.js 把 `_bsearchDanmakuIndex`/`_fmtClock` 兩個純函式從檔案裡抽出來單獨測試（含重複 offset_sec 的 tie-break、before-first/after-last/空陣列邊界），全過。 |
| 3.3 | 點彈幕跳轉 + 關鍵字搜尋跳轉 + 可開關欄 + 時間戳/發言者開關 | 六項互動全可用 | ✅ | ① 點彈幕跳轉：每行 `onclick="seekPlayerTo(offset_sec)"` 設定 `video.currentTime` 並播放；時間戳 `<span class="pl-dm-ts">` 也可單獨點（`stopPropagation` 避免冒泡到整行再觸發一次，值相同不影響結果，純粹避免重複呼叫）。② 關鍵字搜尋：`#pl-dm-search` 輸入框 `oninput="renderPlayerDanmaku()"`，同時比對 `content`/`user`（不分大小寫），結果沿用同一套點擊跳轉/高亮邏輯（搜尋中若目前播放時間點不在篩選結果內，就不高亮任何一行，不會誤高亮，符合直覺）。③ 收合右欄：`☰ 收合彈幕`鈕**故意放在中間播放器控制列**（不在 `#pl-dm-col` 內部）——這樣收合後按鈕仍看得到可以再展開；`togglePlayerDanmaku()` 只是切 `#pl-dm-col` 的 `style.display` 在 `''`/`'none'` 間（跟既有 `toggleBulkMenu()` 手法一致），收合後因為父層是 `display:flex`，中間播放器欄（`flex:1`）自動補滿騰出的空間，**不用額外寫「變大」的 CSS**。④ 時間戳/發言者開關：`#pl-dm-showts`/`#pl-dm-showuser` 兩個 checkbox，`onchange="renderPlayerDanmaku()"` 重繪。⑤ 與既有 VTT 疊字幕並存：完全沒動 `pl-subs`/`applyPlayerSubs()`/`<track>` 那段程式碼，兩者本來就同源（VTT 檔由 `/api/danmaku/subtitle` 從同一個 `danmaku.db` 產生）。**驗證**（無瀏覽器環境）：`html.parser` 逐標籤配對 0 錯誤、EOF stack 清空；`id="..."` 全站唯一性 grep 0 重複（134 個 id，7 個本階段新增：`pl-dm-col/pl-dm-search/pl-dm-list/pl-dm-showts/pl-dm-showuser/pl-dm-count/pl-dm-collapse-btn`，各恰好出現 1 次）；CSS `{`/`}` 169/169 配對；`onclick/onchange/oninput/@click` 呼叫的函式名比對 `function xxx()` 定義清單，0 個缺定義（唯一 regex 誤報是 `event.stopPropagation()`，屬原生方法呼叫非自訂函式，跟階段0記錄的同一類已知誤報模式一致）；三個 `<script>` block 全部丟 `node --check` 語法驗證通過。**與線框的差異（刻意，已判斷風險最低）**：spec §3.4 wireframe 把「日期」和「當天清單」畫成同一個左欄（上下堆疊），本實作維持階段1既有的三欄結構（日期／播放器／當天清單）不變，只在最右邊加第4欄放彈幕——這樣完全不用碰現有的 `pl-dates`/`pl-clips`/`loadDay()`/`renderClips()` 邏輯，把改動面壓到最小（那組程式碼已經是階段1驗證過的既有功能，沒有理由為了排版跟 wireframe 逐像素一致去重構它、引入不必要的迴歸風險）；視覺上四欄用 `flex-wrap:wrap` 排列，窄螢幕一樣會自動換行堆疊，不影響可用性。 |

## 階段 4 — 新頁 + 壓縮占比

**策略決定**：config.ini 讀寫**不用 configparser**，改用跟 `_write_lines()`（URL_config.ini）同款的「原文字 regex 替換」──對每個白名單鍵，只把該鍵那一行 `=` 後面的值替換掉，comment/section/其他鍵/BOM/CRLF 全部原封不動保留。理由：configparser 寫回時會重新序列化整份檔案（改變空白排版、可能弄丟行內註解、依 dict 順序重排鍵），跟「必須保留 config.ini 原有格式」的最高風險要求正面衝突；正則替換則是「連一個位元組都不該動的地方，保證不會動」。壓縮手動觸發則發現一個既有小缺口：`compressor.py` 的背景 coordinator/lane 迴圈本來就是拿 `settings["enabled"]` 當唯一閘門，若使用者把「啟用自動壓縮」關掉，即使呼叫 `trigger_scan()` 喚醒也會在閘門處直接被擋掉、什麼都不會跑──這跟 §3.6「開關只控制是否自動背景執行，關閉時仍可手動觸發」的要求矛盾，於是在 `compressor.py` 加一個新的 `_force_run` 旗標 + `_gate_open(s)` helper（`trigger_scan(force=True)` 設旗標，coordinator 跑完一輪批次後才清掉，讓 lane 執行緒在整個手動批次期間都保持開通），`_force_run` 預設 `False` 時 `_gate_open` 行為與舊版 `s.get("enabled")` 完全等價，所以既有呼叫端（`api_compress_put` 裡 `enabled` 開啟時呼叫的 `trigger_scan()`）行為不變，只有新的手動觸發端點走新分支。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 4.1 | 後端 GET/PUT /api/config（白名單鍵，保留 config.ini 註解/中文鍵） | 讀寫正確、存檔回「需重啟 recorder」旗標 | ✅ | **端點契約**：`GET /api/config` → `{quality, save_path, folder_by_author, folder_by_time, folder_by_title, filename_with_title, video_format, loop_seconds, segment_enabled, segment_seconds, cookies:{<原始cookie鍵>: <值>, ...}, quality_options:[...], video_format_options:[...], exists:true}`；config.ini 不存在回 404 `{error, exists:false}`。`PUT /api/config` body 可送上述任一子集（含巢狀 `cookies:{}`），成功回同形狀 payload 另加 `{changed:[改了哪些欄位id], needs_recorder_restart: bool, message}`；`changed` 永遠只含「payload 有送、且該鍵那一行真的存在於 config.ini」的欄位，未知欄位/不存在的 cookie 鍵/enum 選項不在白名單裡/int 欄位傳非數字 → 靜默忽略（不是 400，因為前端只會送白名單值，這層是防禦性的，寧可忽略也不要讓格式錯亂）。**白名單鍵清單**（`web_ui.py` `CONFIG_FIELDS`，10 個固定欄位 + Cookie 區段全部動態發現）：`quality`→`原画\|超清\|高清\|标清\|流畅`（enum，選項見 `CONFIG_QUALITY_OPTIONS`）、`save_path`→`直播保存路径(不填则默认)`（字串）、`folder_by_author/folder_by_time/folder_by_title`→對應三個「保存文件夹是否以XX区分」（bool，是/否）、`filename_with_title`→`保存文件名是否包含标题`（bool）、`video_format`→`视频保存格式ts\|mkv\|flv\|mp4\|mp3音频\|m4a音频`（enum，見 `CONFIG_FORMAT_OPTIONS`）、`loop_seconds`→`循环时间(秒)`（int）、`segment_enabled`→`分段录制是否开启`（bool）、`segment_seconds`→`视频分段时间(秒)`（int）。Cookie 不是寫死清單，而是 `_config_cookie_keys(text)` 動態掃 `[Cookie]` 區段裡所有 `key = value` 行取得鍵名（目前樣本檔有 50 個，含 `抖音cookie`/`快手cookie`/`tiktok_cookie`.../`picarto_cookie`），GET 回傳 `cookies` 物件的鍵就是這些原始字串，PUT 時只接受「已經在這份清單裡」的鍵——這代表**只能覆寫既有的 cookie 行，不會新增行**，上游以後加新平台 cookie 欄位也會自動被涵蓋，不用改程式碼。**格式保留驗證**：寫了 3 種測試都通過（見下）——(a) 純邏輯測試：對照 `config/config.ini` 樣本檔，改一個鍵（`loop_seconds` 300→250）後逐行 diff，48 項斷言全過，確認「只有目標那一行變了、行數不變、註解(`# 可选微信...`/`# 录制抖音必填`)還在、`[Authorization]`/`[账号密码]` 等後面的 section 都在、bool 來回轉換(是/否)後文字位元組級相同、enum 傳錯值/int 傳非數字時整份文字完全不變（0 byte 差異）、路徑含反斜線（`F:\new\path\with\g<1>\backslashes`）精確保留不被 regex backreference 誤解——這也是為什麼替換用 `pattern.sub(lambda m: ...)` 函式而不是字串 backref」；(b) 真的用 Flask test client + 真實 SQLite/檔案跑了一次端到端：`PUT /api/config {"loop_seconds":999}` 後重新讀檔，磁碟上該行變成 `循环时间(秒) = 999`、BOM (`EF BB BF`) 保留、其餘 130 行不動。`py_compile`（複製到 /tmp 再跑，避免掛載截斷誤報）通過。 |
| 4.2 | #settings/recording 錄製設定頁（P1-1，§3.6） | 畫質/路徑/分資料夾/格式/循環/分段/cookie 可存 | ✅ | `web_static/index.html` 的 `#view-settings-recording` 從階段0佔位換成完整表單：畫質/影片格式用 `<select>`（選項來自 `/api/config` 回傳的 `quality_options`/`video_format_options`，若目前值不在選項裡會動態多加一項「目前值，不在常見選項中」，不會讓使用者的既有設定憑空消失）、保存路徑是純文字輸入框（**沒有做原生資料夾瀏覽器**——瀏覽器沙箱本來就不給網頁應用開系統原生目錄選擇窗且回傳完整路徑，`<input type=file webkitdirectory>` 也只給檔名不給絕對路徑，硬做假的瀏覽鈕比誠實地放一行提示文字風險更高，故用 `c-form-hint` 說明「請直接貼上完整路徑」）、分資料夾三個 checkbox + 檔名含標題、循環時間/分段開關+秒數、Cookie 用 `<details>` 收合（§3.6 線框的「[Cookie 設定 ▾]」），內容是 JS 依 `/api/config` 回傳的 `cookies` 物件動態產生一排排 `<label>+<input>`（不是寫死 30 幾個平台的 HTML，這樣後端白名單多了新平台，前端自動就有欄位，兩邊永遠同步）。JS：`loadRecordingConfig()`（進頁時 lazy-load，`onShellRouteChange` 裡 `route==='settings/recording' && !RC_LOADED` 才觸發，config.ini 讀取有 IO 成本，不用像 alerts 那樣 eager-load）/`_recordingConfigPayload()`（組 PUT body，含收集所有 `.rc-cookie-input` 的 `data-key`→value）/`saveRecordingConfig()`（PUT 後把 `message`/`needs_recorder_restart` 顯示在儲存列，並 toast），底部儲存列文案固定顯示「⚠ 部分設定需重啟錄製器（DouyinLiveRecorder.exe）才會套用」符合 §3.6 要求。config.ini 不存在時顯示 `c-empty` 錯誤態而不是空表單。 |
| 4.3 | 後端 GET /api/compress/overview（全部/已壓縮/未壓縮 占比，快取 60s）+ 壓縮頁占比 panel + 手動「立即開始」 | 占比正確、手動觸發可用、自動開關獨立 | ✅ | **端點契約**：`GET /api/compress/overview` → `{total, total_bytes, compressed, compressed_bytes, pending, pending_bytes, compressed_ratio, saved_bytes, cached_at}`（欄位比 §6 要求的 `{total,compressed,pending,compressed_ratio,saved_bytes}` 多附帶位元組數與快取時間戳，純加法不影響契約）。**掃描邏輯**：新增 `src/compressor.py::scan_overview(save_path, settings)`（沿用 `scan_candidates()` 同一套 `DEFAULT_COMPRESS["extensions"]`/`["suffix"]` 判斷規則，避免兩邊定義漂移）——檔名副檔名在白名單內、且不含 `_hevc.part`（進行中的暫存輸出，兩邊都不算）才計入 `total`；其中檔名主體以 `_hevc` 結尾的算 `compressed`，其餘算 `pending`。`saved_bytes` **不是**從這次掃描推算（掃描只看得到現在的檔案大小，不知道已被取代的原始檔案原本多大），而是直接讀 `compressor.get_status()` 既有的 `stats.saved_bytes`（累計 `in_bytes-out_bytes`，壓縮 pipeline 自己在記）。**快取**：`web_ui.py` 用獨立的 `_compress_overview_cache = {"data","at"}` + 60s TTL（跟 `get_folder_size_cached` 同一種「dict + 時間戳」快取形狀，但不是同一份快取，因為這裡要的是全部/已壓/未壓三種位元組總量，`_size_cache` 只存單一資料夾總大小），`/api/compress/scan`（手動觸發）跟舊行為一樣會 `_size_cache.clear()`，新增也會清 `_compress_overview_cache`，讓使用者按下「立即開始壓縮」後占比 panel 別再顯示 60 秒前的舊快取。**手動觸發獨立於自動開關**：`/api/compress/scan` 改呼叫 `_compressor.trigger_scan(force=True)`（見上方策略說明的 `_force_run`/`_gate_open` 機制），即使 `compress.json` 裡 `enabled=false`，這個端點還是會讓 coordinator 真的跑一輪掃描+壓縮，而不是被閘門靜默擋掉——這其實順便修掉一個既有的行為缺口（舊版「立即掃描」鈕在自動壓縮關閉時其實完全沒作用，只是 UI 上看不出來）。前端：`#view-settings-compress` 既有的「立即掃描」鈕改標籤成「▶ 立即開始壓縮」+ title 提示獨立於開關、旁邊加一行 `c-form-hint` 說明；新增「壓縮總覽（占比）」panel（`loadCompressOverview()`，畫兩段式進度條：已壓縮/未壓縮 + 檔數/位元組明細 + 累計已省空間），在 `loadCompress()`（頁面載入）跟 `scanCompress()`（手動觸發後 2 秒）各呼叫一次，**不**掛在既有 `CP_TIMER`（10s 輪詢）上——後端有 60s 快取，10s 輪詢只會一直打到快取、沒有意義，維持「輕量、不多打不必要的 API」原則。 |
| 4.4 | #stats 統計頁（P1-2，§3.5：排行/摘要/單主播明細/匯出） | 資料對應 duration_tracker 查詢 | ✅ | **端點契約**（新增於 `web_ui.py`，都用既有 `_db_query()` 直連 `recording_history.db`，**沒有**改用 `duration_tracker.DurationTracker`——理由跟檔案裡其他統計函式（`totals_today`/`grand_total`/`_per_url_totals`）一致：那個類別用 `Path(__file__).resolve().parent.parent` 自己算 ROOT，跟 web_ui.py 的 `ROOT`（source 目錄 vs 打包 exe 目錄可能不同，見 AGENT.md §3）不保證一致，直接查 DB 比較安全）：`GET /api/stats/overview?range=all\|month\|today&since=YYYY-MM-DD&limit=N` → `{range, since, summary:{total_sec,total_sessions,avg_session_sec,longest_session_sec,anchors,today_sec,month_sec}, ranking:[{anchor_name,total_sec,sessions}, ...]}`（`since` 自訂日期會覆蓋 `range`）；`GET /api/stats/anchor?name=X&range=&limit=` → `{anchor_name, total_sec, session_count, sessions:[{id,platform,live_url,quality,start_time,end_time,duration_sec,file_path,finished_reason}, ...]}`；`GET /api/stats/export?range=&since=` → 直接回 CSV（`Content-Disposition: attachment`），欄位/順序跟 `duration_tracker.DurationTracker.export_csv()`/`query_duration.py --export` 一致（主播/平台/URL/畫質/開始/結束/時長秒/時長h:m:s/檔案/結束原因），一樣用 utf-8-sig BOM 讓 Excel 開中文不亂碼。SQL 形狀直接對照 `query_duration.py`：排行榜＝`total_by_anchor()` 的 `GROUP BY anchor_name ORDER BY total DESC`、單主播明細＝`sessions_for()` 的 `ORDER BY start_time DESC`、`end_time IS NOT NULL` 過濾條件（進行中的場次不計入時長統計，跟舊 CLI 行為一致）都逐一比對過。前端 `#view-stats`：篩選列（全部歷史/本月/今日 + 匯出 CSV 連結，`<a download>` 直接觸發瀏覽器下載，不用額外 JS 組 blob）、排行榜 table（點主播名字展開明細，複用同一個 `esc()` 跳脫慣例，跟既有 `stopRecording('${esc(...)}')` 等呼叫模式一致）、摘要 panel、單主播明細 panel（`showStatsDetail()`/`closeStatsDetail()`，切換範圍篩選時若明細面板開著會同步重新查詢）。`onShellRouteChange` 加 `route==='stats' → initStats()` dispatch。 |

**驗證方式**（本階段全部都做了，沒有可用瀏覽器環境下盡量做端到端）：後端複製到 `/tmp` 後 `python3 -m py_compile` 通過（`web_ui.py`/`src/compressor.py`）；純邏輯 pure-function 測試共 67 項斷言（config.ini 讀寫 48 項、`compressor.scan_overview` 6 項、stats SQL helper 13 項）全過；額外裝了 `requirements.txt` 全部依賴 + Flask，把整個 `web_ui.py`（含 `src/` 全套模組）在乾淨環境用 `WEB_UI_ROOT` 指向臨時目錄（自建 `config/config.ini`+`recording_history.db`+假錄影檔）跑 Flask test client，端到端打了 `GET/PUT /api/config`、`GET /api/compress/overview`、`POST /api/compress/scan`、`GET /api/stats/overview|anchor|export` 全部 6 個新端點，回應內容跟 pure-function 測試預期完全吻合，且確認磁碟上 config.ini 只有目標那一行變、BOM 保留。前端：`html.parser` 逐標籤配對 0 錯誤、id 全域唯一性檢查（166 個 id 全部恰好 1 次）、CSS `{`/`}` 169/169 配對、226 個 `getElementById` 全部能在 DOM 找到目標、onclick/onchange/oninput/@click 呼叫的函式名比對定義清單 0 個真缺失（1 個 regex 誤報：動態組 HTML 字串裡混進的 JS 原生 `.replace()`，跟階段0/3 記錄的同一類已知誤報模式一致，且已順手簡化掉那行多餘的轉義邏輯）、三個 `<script>` block 全部 `node --check` 語法通過。

## 階段 5 — 技術債清理（§9；可穿插）

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 5.1 | 抽 src/common.py：deep_merge / load-save json settings / http_post_json / fmt_duration | 各呼叫端改用、行為等價、既有測試過 | ✅ | 新增 `src/common.py`（純標準庫）：`deep_merge`、`load_json_settings(path, defaults)`/`save_json_settings(path, data, defaults)`、`http_post_json(url, payload, proxy, ua)`→`(ok, detail)`、`fmt_duration(sec)`→`"H:MM:SS"`。**`_deep_merge`／`load_settings`／`save_settings`**：`alerts.py`（`load_alerts`/`save_alerts`）、`compressor.py`、`rec_notify.py` 三份改成呼叫 common，`_deep_merge` 保留成 `_deep_merge = _common.deep_merge` 別名（不改動三檔內部既有的十幾處 `_deep_merge(...)` 呼叫點，降低改動面）。**`http_post_json` 難點**：`alerts.py` 原本的 `_post_json` 永遠**強制不用 proxy**（`ProxyHandler({})`）且不帶 UA、回傳純 `bool`；`rec_notify.py` 原本的 `_post_json` 預設**沿用系統/環境 proxy**、有設定才走顯式 proxy、固定帶 Discord UA（Cloudflare 需要）、回傳 `(ok, detail)` 且會寫入模組全域 `_last_error`。兩者行為不等價，硬合併會靜默改變其中一方的網路行為，所以 `http_post_json` 的 `proxy` 參數設計成**三態**：`None`＝強制不用 proxy（給 alerts.py）、`""`（預設）＝沿用系統/環境 proxy（給 rec_notify.py 未設定時）、非空字串＝顯式 proxy（給 rec_notify.py 有設定時）；`ua` 參數預設 `None`＝不帶 UA（沿用 alerts.py 原行為），rec_notify.py 呼叫時顯式傳入原本寫死的 Discord UA 常數（改名 `_DISCORD_UA`，字串逐字元相同）。`alerts.py._post_json` 改寫後**仍回傳 bool**（在內部 unpack `(ok, detail)` 只取 `ok`）——因為原呼叫端 `_dispatch()` 用 `and _post_json(...)` 短路布林判斷，若直接回傳 tuple 會永遠 truthy 變成靜默 bug，這點在測試中特別驗證。**`fmt_duration`**：只抽了真正逐位元組相同的一組——`duration_tracker.py` CSV 匯出的 `str(timedelta(seconds=dur))` 與 `query_duration.py._fmt()`（本來就是同一行邏輯複製兩份），加上追蹤到 stage4 新增、同樣寫成 `str(timedelta(...))` 的 `web_ui.py` `/api/stats/export`（三處合併，改用 `_common.fmt_duration`，`web_ui.py` 用 guard `_common.fmt_duration(dur) if _common else str(timedelta(seconds=dur))` 維持既有的 try/except-import-None 容錯慣例）。**刻意不抽**：`rec_notify.py._fmt_dur()`（緊湊格式 `1h02m`/`5m23s`，Discord 卡片用）與 `danmaku_subtitle.py` 的 `_fmt_ass_time`/`_fmt_srt_time`（字幕 cue 時間戳 `HH:MM:SS.cs`/`HH:MM:SS,ms`）——三者輸出格式與用途都不同，抽在一起或改參數化都會有「一份程式碼、行為靠 if 分岔」的壞味道，且不影響「消除重複」的目標（它們本來就不是重複，只是名字很像），故保留原樣，符合任務說明「若無法完全等價就保留原樣不硬抽」。`get_folder_size_cached`／`_size_cache` 未被抽動——確認 stage4 新增的 `_compress_overview_cache`（`/api/compress/overview` 用）是獨立設計，理由已寫在 `web_ui.py` 該處註解（要的是總/已壓/未壓三種位元組拆分，跟單一資料夾總大小的快取形狀不同），沒有二次實作同一件事，符合「沿用同一份、別再寫一套」的要求，不需改動。**驗證**：`src/common.py` 4 個函式各自的等價性單元測試（`deep_merge` 對照舊 inline 實作、`load/save_json_settings` round-trip + 損毀檔/非 dict fallback、`http_post_json` 用 monkeypatch `urllib.request.build_opener` 驗證三種 proxy 分支各自呼叫對的 handler、`fmt_duration` 對照 `str(timedelta(...))` 掃 10 組值全過）；`alerts.py`/`compressor.py`/`rec_notify.py` 委派後的行為測試（`load_alerts`/`save_alerts` round-trip、`_deep_merge is _common.deep_merge` 別名檢查、`alerts._post_json` 回傳 `True`/`bool` 型別且強制無 proxy、`rec_notify._post_json` 回傳 tuple 且正確帶 Discord UA／依 `_proxy_url` 切換 proxy 分支）；`duration_tracker.py` 用真實 SQLite 跑一次 `session_start`→`session_end`→`export_csv`，CSV 的「時長(h:m:s)」欄位輸出 `0:00:01` 與改動前公式一致。全部檔案（`src/common.py`/`alerts.py`/`compressor.py`/`rec_notify.py`/`duration_tracker.py`/`query_duration.py`/`web_ui.py`）複製到獨立環境 `py_compile` 通過（大檔另外用 `wc -l` 比對行數確認掛載沒截斷）。 |
| 5.2 | 移除死碼：_now_hm、改批次後的單則 embed/text、孤兒 diff_issue_codes、未用 import | grep 確認零呼叫端後移除 | ✅ | 全專案（含 `web_static/` JS）grep 確認**零呼叫端**後移除：`rec_notify.py` 的 `_now_hm()`、`format_start()`/`format_stop()`（單則純文字，已被 `format_batch_text` 取代）、`build_start_embed()`/`build_stop_embed()`（單則 embed，已被 `build_batch_embed` 取代）——先確認過 `process_active()`（背景輪詢主流程）與 `send_test()`/`send_current_status()` 皆已改用批次版或專用 test_embed，沒有任何路徑還在用這 5 個舊函式，才動手刪，不是兩套並存去掉一套。`health.py` 的 `diff_issue_codes()`——全專案零呼叫端，直接移除（沒有被 watchdog 或任何端點接上）。**未用 import 清理**（`pyflakes` 掃過本次改動的 7 個檔案 + `web_ui.py`）：`alerts.py` 移除 `import json`、`from pathlib import Path`（改用 common 後兩者在本檔不再需要）；`rec_notify.py` 移除 `import json`、`from pathlib import Path`、`import urllib.request`、`import urllib.error`（同上，改用 common 後不再直接碰 urllib/json/Path）、`typing.Callable`（pyflakes 抓到的既存孤兒 import，非本次改動造成，順手清掉）；`duration_tracker.py` 移除 `from datetime import timedelta` 中的 `timedelta`（改成 import datetime 不含 timedelta，因為改用 `common.fmt_duration` 後本檔不再需要）與既存孤兒 `import os`（pyflakes 抓到，本檔從未用過 `os.`，順手清掉）；`query_duration.py` 移除 `timedelta`（同理）；`web_ui.py` 移除既存孤兒 `import configparser`（stage4 決定改用 regex 逐行替換寫 config.ini 而非 configparser 後，這個 import 就沒人用了，pyflakes 抓到順手清掉，`timedelta` 本身在 web_ui.py 其他地方仍有用不動）。所有清理後 `pyflakes` 對這批檔案重跑皆為 0 警告（除 `web_ui.py` 全檔本身可能有的其他既存無關警告未逐一清，不在本次改動範圍內）。**未動**：`get_folder_size_cached`/`_size_cache`（見 5.1 說明，非死碼，是刻意獨立的設計）；`compressor.py`/`health.py` 其餘 import 皆有使用，`pyflakes` 掃過乾淨。 |
| 5.3 | widget 標 deprecated（README 註明、autostart 不裝；是否刪檔待使用者定） | 文件更新、不為 widget 做新 UI | ✅ | 確認 `install_autostart.bat` 現況：**本來就已經不裝 widget**（該檔第76-79行的既有註解已寫明「舊版會多裝一個 DLR-Widget.lnk...如果是從舊版升級上來，清掉殘留的那個捷徑」，且腳本主體只建立 `DLR-Recorder.lnk`/`DLR-WebUI.lnk` 兩個捷徑），不需要改程式碼，只是確認並在文件記錄這件事。README.md 更新：功能列拿掉獨立的「桌面小工具」條目、改成在「開機自啟」條目註明「桌面小工具已棄用，見下方說明」；目錄架構表對 `widget.py`/`widget_window.py`/`start_widget.bat`/`web_static/widget.html`/`webui_port.txt` 註解加上「【已棄用】」標記並說明現況（功能已併入瀏覽器控制台）；原本獨立的「## 用桌面 widget」章節整段移除，改寫進新的「## 用 Web UI」章節說明（見 5.4）；疑難排解區的「Tray icon 沒出現」/「浮動視窗顯示 Not Found」兩條合併成一條「（舊版）」條目，說明 widget 已棄用、一般情況不需理會這兩個問題；多處建議「執行 `start_widget.bat` 重啟」改成「執行 `start_console.bat`」（日常建議用的啟動方式）。ROADMAP.md／PROJECT_STATUS.md 同步把「Web 控制台 + 桌面 widget」的定位描述改成「Web 控制台；桌面 widget 已棄用」，並在 ROADMAP §6 近期建議順序新增一條「（待決）...是否直接刪除留給使用者決定」。**不刪檔**：`widget.py`/`widget_window.py`/`web_static/widget.html`/`start_widget.bat` 全部保留在原位，只加棄用標記，符合任務說明「是否刪檔由使用者決定」；`start_widget.bat` 仍可正常手動執行給想用舊浮動視窗的人。 |
| 5.4 | 文件同步：README（新 UI 操作）+ ROADMAP（P1-1/P1-2 標完成）+ PROJECT_STATUS（補條） | 三文件一致 | ✅ | **README.md**：功能列新增「統計頁」「錄製設定頁」條目、壓縮條目補上「壓縮占比總覽」與獨立手動觸發說明、回放條目補上「同步彈幕」說明；「用 Web UI」章節整段重寫，改成描述側邊欄導覽（監控/內容/設定三組）+ 逐一列出 10 個 view（儀表板/主播管理/彈幕查詢/錄影回放/統計/5 個設定子頁）+ topbar／側邊欄常駐迷你狀態；新增「## 錄製設定頁」「## 統計頁」兩個獨立章節說明操作方式；原本散落各處指向舊版彈出式「⚙ 通知設定」面板的文字（Discord 通知/健康警報/磁碟管理/自動壓縮四個章節）全部改成指向對應的側邊欄「設定 → 通知／壓縮／磁碟」子頁；「設定每位主播的錄製上限」章節的入口敘述改成「側邊欄『主播管理』」（呼應 stage1.2 的改名）。**ROADMAP.md**：P1-1／P1-2 從「🟠 P1 — 中期（待做）」清單移除，改成刪除線註記指向新增的「✅ 已完成（2026-08 批次）— 控制台 UI/UX 重構（含 P1-1、P1-2）」章節（內容涵蓋五個階段摘要 + 技術債清理項目）；現況快照表格的第5/6項缺口（設定要手改 .ini、好東西藏在 CLI）標記已解並說明對應的新頁面；優先級矩陣 ASCII 圖的「P1：設定 GUI／歷史統計頁」補上「(已完成)」；§6 近期建議順序更新剩餘項目編號（現在只剩 P1-3 首次設定精靈 + P2 + widget 刪檔待決）。**PROJECT_STATUS.md**：最後更新日期改 2026-08-26、文件導覽補上 `UIUX_SPEC.md`/`UIUX_TASKS.md`；新增「2.10 控制台 UI/UX 重構」小節（內容同 ROADMAP 的完成摘要，另外補上 5.1 的驗證方法摘要，方便交接時不用回頭翻 UIUX_TASKS.md 的長篇備註）；「下一步建議」章節移除已完成的 P1-1/P1-2；「已知限制」章節的 `start_widget.bat` 重啟建議改成 `start_console.bat`。三份文件互相 grep 交叉檢查過，沒有殘留「P1-1/P1-2 待做」或未加註記的裸 widget 使用建議。 |

## 階段 6 — 破版修復：全域 CSS 洩漏 + 統計頁載入失敗（2026-08-26）

**背景**：階段1把舊版 `<header>/<nav>/<main><section id="page-*">` DOM 整段拆掉、內容搬進 `#new-shell` 底下的新框架後，檔案頂部（第13~131行）那批**舊版全域元素選擇器**（`header{}`/`nav{}`/`main{}`/`section{}` 等，非 `#new-shell` scope）沒有一併清掉；因為全站現在只剩新框架這一份 DOM，這些選擇器不再有「舊版另一份 DOM」可以安全對應，變成 100% 命中新框架同名元素、造成樣式洩漏破版。同時統計頁在特定資料庫狀態下會回 500，前端顯示「載入失敗」。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 6.1 | CSS 洩漏根因修復：移除孤兒全域 `header{}`/`header h1{}`/`header .meta{}`/`nav{}`/`nav button{}`/`nav button.active{}`/`main{}`/`section{}`/`section.active{}` | 側邊欄三群組恢復垂直堆疊、主內容區滿版不置中限寬、其餘頁面不受影響 | ✅ | **逐條確認過全站只剩一個 `<header>`/`<nav>`/`<main>`、且 `<section>` 已完全不存在**（grep 全文件，僅新框架的 `<header class="c-topbar">`／`<nav class="c-sidebar-nav">`／`<main class="shell-content">` 各一份；`<section>` 只留在註解裡）。**具體洩漏機制**：① `nav { display:flex; gap:4px; padding:0 24px; ... }`（原第29行）——`#new-shell .c-sidebar-nav` 本身沒有設 `display`，於是這條全域規則的 `display:flex` 直接生效在側邊欄 `<nav class="c-sidebar-nav">` 上，把裡面三個 `.c-sidebar-group`（監控/內容/設定）從預期的垂直堆疊（block 預設）強制排成水平一列，這就是側邊欄破版主因；`nav button`/`nav button.active` 因為新版 `<nav>` 底下已全部改成 `<a class="c-sidebar-link">`（不是 `<button>`），純屬死碼一併清掉。② `main { padding:24px; max-width:1600px; margin:0 auto; }`（原第48行）——`#new-shell .shell-content` 有設 `padding`（`var(--sp-5)`，靠 `#new-shell .shell-content` 選擇器 specificity `(1,1,0)` 高於裸元素選擇器 `main` 的 `(0,0,1)` 而覆蓋掉），但**沒有設 `max-width`/`margin`**，於是這兩個屬性從全域 `main{}` 規則洩漏進來：`.shell-content` 是 `.shell-main`（`flex-direction:column`）底下的 flex item，被限成 `max-width:1600px` 又 `margin:0 auto` 置中，寬螢幕下會在內容區左右兩側各自擠出等寬留白、視覺上像「內容被擠到中間/偏窄」，這就是主內容區留白的成因。③ `header { ...; justify-content:space-between }`（原25-26行）——`#new-shell .c-topbar` 覆蓋了 padding/background/border/display/align-items（同值或有設定），但沒設 `justify-content`，故 `space-between` 洩漏進 topbar，雖非螢幕截圖裡的主要症狀，仍一併清除避免後續改版時才發作的潛伏 bug；`header h1`/`header .meta` 經 grep 確認新框架裡沒有 `<h1>`、且僅有的三個 `class="meta"` span 都不在 `<header>` 底下（在各頁 toolbar 裡），純屬死碼一併清掉。**確認未動到仍在用的規則**：`body{}`（新框架有自己的 `#new-shell{ background:var(--bg); ... }` 以 `position:fixed;inset:0` 整頁覆蓋，`body` 規則基本不可見但保留無害）、`table{}`/`th,td{}`/`th{}`（grep 確認全站 6 個 `<table>` 都**沒有**加 `class="c-table"`，是真的還在吃這組全域規則做基礎樣式，不能刪）、`.card/.btn/.pill/.switch/.modal/.toast/.empty/.savebar/.toolbar/.search/.field*` 等舊 class 規則（`renderStreamers()`/`refreshDashboard()` 等 JS 樣板字串與靜態 HTML 仍大量使用，逐一 grep 計數確認非零使用，維持原樣）。修復後 CSS `{`/`}` 計數 162/162 配對，靜態確認：新框架裡再也沒有裸元素選擇器可以命中除 `body`/`table`/`th`/`td` 外的任何內容。 |
| 6.2 | 統計頁「載入失敗」根因修復：`_db_query()` 對 `no such table` 容錯 | 空 DB／無 DB／DB 存在但表未建 三種情況 `/api/stats/*` 皆回 200 空結果，不再 500 | ✅ | 用 Flask test client 對四種 DB 狀態實測 `/api/stats/overview\|anchor\|export` 找出真正原因：(a) 正常有資料——皆 200 且數值正確；(b) 表存在但 0 筆——皆 200 空結果；(c) `recording_history.db` 檔案完全不存在——`_db_query()` 本來就有 `if not DB_FILE.exists(): return []` 擋掉，200 空結果；(d) **`recording_history.db` 檔案存在、但 `recording_sessions` 表還沒建立**（例如剛安裝、web_ui 在錄製器第一次寫入前就先被打開、或 config 目錄裡留了一個空白 placeholder 檔）——這個情況下 `con.execute(sql, params)` 直接拋 `sqlite3.OperationalError: no such table: recording_sessions`，沒有任何 except 接住，Flask 回應變成沒有 JSON body 的裸 HTML 500 頁；前端 `api()` 對非 2xx 回應嘗試 `r.json()` 失敗、`catch(()=>({}))` 吞掉錯誤細節後直接 `throw`，`loadStats()` 的 catch block 就顯示「載入失敗」——這就是螢幕截圖看到的真正成因，且不只影響統計頁，`_db_query()` 是 `totals_today`/`grand_total`/`_per_url_totals` 等其他既有函式共用的同一個 helper，理論上一樣會中招（只是這些函式的呼叫端多半已有各自的容錯，恰好沒被使用者注意到）。**修法**：在 `_db_query()` 裡對 `sqlite3.OperationalError` 加一層 catch，訊息含 `"no such table"` 時視同「還沒有任何資料」回傳 `[]`，其餘錯誤原樣 re-raise（不吞掉未知例外，避免掩蓋真正的資料庫損毀等問題）。**沒有改動 schema 建立權責**——建表邏輯仍然只在 `src/duration_tracker.py`（`DurationTracker._init_db()` 的 `CREATE TABLE IF NOT EXISTS`），`web_ui.py` 維持純唯讀，不在這裡搶著建表，符合「後端只修 bug 不改行為」的限制。**驗證**：`python -m py_compile`（複製到暫存目錄）通過；Flask test client 對上述四種 DB 狀態（含新增的「檔案存在但無 schema」情境）逐一重測 `/api/stats/overview?range=all\|today\|month`、`/api/stats/anchor`、`/api/stats/export`，修復後全部回 200，情境(d)修復前是 500、修復後正確回傳空的 summary/ranking／CSV 只有表頭；額外抽測 `/api/status`、`/api/streamers` 在「表未建」情境下也維持 200（原本可能有同款風險，這次一併受益於同一個 helper 修復，未特地為它們寫獨立分支）。 |

**與階段1備註的落差**：階段1當時的策略說明寫「舊版視覺 class...沿用不變、繼續生效」，這句話本身沒錯（class 規則確實該留），但漏了一併檢查「舊版全域**元素選擇器**（非 class）在新框架也有同名元素」這個情況——這兩種選擇器的風險模型不同：class 選擇器只命中「還在用該 class 的元素」，天然安全；元素選擇器命中「所有同名標籤」，舊 DOM 拆掉、新 DOM 用了同名標籤時就會直接洩漏。這是本次破版的根本原因，記錄於此供之後改版時參考（**規則**：拆舊 DOM 時，全域 `<style>` 裡的裸元素選擇器要跟著重新審視，不能只看 class 選擇器）。

---

## 階段 7 — 側邊欄排版重寫（2026-08-26）

**背景**：階段6修完 CSS 洩漏後，使用者回報「sidebar 排版問題太多」，具體點出：圖示與文字擠在一起/重疊、文字被折成多行、群組標題（監控/內容/設定）顯示怪、整體寬度/對齊/排版亂。使用者明確指示**直接重寫 sidebar 的 CSS/結構**，不再派 agent 猜修。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 7.1 | 側邊欄連結改成 icon/label 兩個獨立 span + nowrap | 圖示固定對齊、文字不折行、群組標題單行 | ✅ | **根因**：每個連結的 emoji＋空格＋中文原本是同一個文字節點（如 `&#128202; 儀表板`），`gap` 對單一文字節點無效（沒有東西可以「隔開」），且沒設 `white-space:nowrap`，220px 窄側欄下中文會不可預期地折行（「儀表板」拆成兩行）。**修法**：CSS 新增 `.c-sidebar-ico`（固定 20px 寬、置中，圖示永遠對齊）+ `.c-sidebar-label`（`flex:1; min-width:0; overflow:hidden; text-overflow:ellipsis`，真的過長才省略號、不折行）；`.c-sidebar-link/.c-sidebar-group-title/.c-sidebar-brand/.c-sidebar-status` 全部加上 `white-space:nowrap; overflow:hidden`；`.c-sidebar-status` 從 `flex-wrap` 橫排改成 `flex-direction:column` 直排（錄製器狀態、磁碟各自一行，不再擠在一起折行）；`.c-sidebar-brand` 固定高度 52px 對齊 topbar。HTML 同步把 10 個 `<a class="c-sidebar-link">` 內容改成 `<span class="c-sidebar-ico">...</span><span class="c-sidebar-label">...</span>` 兩個獨立 span；群組標題從中英雙語「監控 MONITOR」等縮短成純中文「監控/內容/設定」（縮小視覺負擔、11px 更不易擠爆）；播放圖示拿掉 variation-selector 尾碼（`▶️`→`▶`，避免不同字型渲染寬度不一致）。**驗證**（無瀏覽器環境，純靜態）：CSS 大括號配對 809/809；HTML 無重複 id；新增的 5 個 class（`c-sidebar-ico/c-sidebar-label/c-sidebar-group/c-sidebar-link/c-sidebar-status`）皆同時存在於 CSS 定義與 HTML 使用；`html.parser` 標籤配對 0 錯誤。**限制**：沙箱無瀏覽器，無法用截圖實際肉眼確認渲染結果，已請使用者重新整理後目視確認。 |

---

## 階段 8 — 回放頁改「先選主播、再選日期」（2026-08-26）

**背景**：使用者回報回放頁點擊後不會播放影片；追問後使用者更正真正想要的行為：**回放應該先選主播、再選日期**（不是像原本一樣把所有主播的錄影混在同一份「依日期」清單裡）。原設計下 `/api/library/dates`／`/api/library/day` 回傳的是**全站**（不分主播）依日期分組的清單，日期相同、不同主播的片段會混在同一天的「當天清單」裡，使用者難以定位到特定主播特定場次，也是先前「點了看起來沒反應」的體感來源之一。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 8.1 | 回放頁新增「主播」選擇欄，日期/當天清單改為依選定主播過濾 | 進頁先看到主播清單→選主播才出現該主播的日期清單→選日期才出現當天清單→自動播放第一段 | ✅ | **後端**：`src/library.py` 新增 `list_anchors(sessions)`（純函式，依 `anchor_name` 分桶統計場次數/總時長/最近錄製日期，依最近日期新到舊排序，`anchor_name` 為空時歸入 `"(未知主播)"` 桶）。`web_ui.py` 新增 `GET /api/library/anchors`（回傳主播清單）、新增共用的 `_sessions_for_anchor(anchor)` helper（`anchor` 空字串＝不過濾，回傳全部），`GET /api/library/dates`／`GET /api/library/day` 都新增可選的 `anchor` query 參數、內部改呼叫 `_sessions_for_anchor(anchor)` 取代原本直接呼叫 `_finished_sessions_for_library()`，同一顆 `id_for=_clip_id` 沒有變動（clip id 產生方式不變，不影響既有的 `/api/library/video/<id>`／`/api/library/clip/<id>/danmaku` 端點）。**前端**：`#view-player` 最左側新增「主播」欄（`#pl-anchors`，170px），「依日期」欄提示文字改成「請先選擇主播」；新增 `PL_ANCHOR`/`PL_ANCHORS` 狀態、`renderAnchors()`、`selectAnchor(name)`（切主播時重置 `PL_DATE/PL_CLIPS/PL_INDEX`、清空播放器與同步彈幕欄、重新載入該主播的日期清單、自動選最新一天）；`initPlayer()` 改成先抓 `/api/library/anchors`、自動選最近活動的主播（`PL_ANCHORS[0]`），且用 `if (PL_ANCHOR) return` 擋掉「切分頁再切回來」重複重置已選狀態；`loadDay(date)` 呼叫 `/api/library/day` 時帶上 `anchor=` 參數。**XSS/跳脫注意**：主播名稱是 Douyin 上的真實顯示名稱、可能含引號等特殊字元，不同於原本固定格式的日期字串（`YYYY-MM-DD` 直接內插進 `onclick="loadDay('${d.date}')"` 是安全的），因此主播按鈕**不**把名稱直接內插進 onclick 的 JS 字串，而是先用既有的 `esc()` 寫進 `data-anchor="${esc(a.anchor_name)}"` HTML 屬性（`esc()` 對 `&<>"'` 都會跳脫），再用 `onclick="selectAnchor(this.dataset.anchor)"` 讀屬性值——名稱只會以「HTML 屬性值」身分出現，不會被拼進任何 JS 字串字面值，從結構上避開了引號斷字串的注入風險。**驗證**：`web_ui.py`／`src/library.py` 複製到暫存目錄後 `py_compile` 通過；`index.html` 大括號配對 820/820、無重複 id、`html.parser` 標籤配對 0 錯誤、抽出 `<script>` 內容以 `node --check` 驗證 JS 語法通過；grep 確認 `selectAnchor/renderAnchors/initPlayer/loadDay` 皆有定義、`onclick="selectAnchor` 恰好 1 處呼叫來源（button 樣板）。**限制**：沙箱無瀏覽器，無法實際點擊驗證播放行為，也未能重現使用者原本回報的「點擊沒播放影片」根因（`/api/library/video/<id>` 的 `send_file(conditional=True)` 邏輯本身沒有改動）；若改成先選主播後仍有「選了片段但影片不播放」的情況，需要使用者提供瀏覽器 F12 Console／Network 的錯誤訊息或截圖才能進一步定位（可能方向：影片格式瀏覽器不支援、檔案路徑解析失敗回 404、或是別的前端事件綁定問題）。 |

---

## 階段 9 — 回放頁清單加篩選/排序/可調欄寬 + 放不出影片的診斷欄位（2026-08-26）

**背景**：使用者實測階段8的「先選主播」流程後，直接打 `/api/library/day?date=2026-08-26&anchor=...` 這條 API，回傳 `{"clips":[],"date":"2026-08-26"}`——日期清單明明有段數，選進去卻一段都放不出來，且「全部都是這樣」（不只這一個主播/日期）。同時使用者提出新需求：三個清單（主播/日期/當天清單）要能篩選、排序、拖曳調欄寬。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 9.1 | 追查「有紀錄卻放不出影片」：加診斷欄位而非用猜的 | `/api/library/day` 在「查得到紀錄但一段都放不出來」時附上 `debug` 陣列 | ✅ | **無法直接重現**：沙箱看不到使用者實機的 `recording_history.db`／實際錄影檔案（掛載的資料夾是原始碼 `source_with_duration_tracker`，執行期的 `config/`＋錄影檔在其上層安裝目錄，本來就不在我能存取的範圍）。改追程式邏輯：`build_playlist()`（`library.py`）在 `resolve_video()` 找不到任何候選檔案（`<stem>_hevc.mp4`／原始路徑／`<stem>.mp4\|.mkv\|.flv\|.ts`／`<stem>_hevc.<ext>`）時會靜默 `continue` 跳過該筆——這解釋了「資料庫有紀錄、日期清單有段數，但當天清單是空的」的現象，但無法在沒有實機資料的情況下判斷**根本原因**是路徑格式對不上、檔案被搬走/刪除、還是轉檔失敗留下的殘檔。**修法**：`/api/library/day` 改成——當回應的 `clips` 是空陣列、但該日期＋主播篩選條件下**資料庫確實有符合的紀錄**時，額外附上 `debug` 欄位，逐筆列出 `anchor_name/start_time/stored_file_path/stored_path_exists`（原始路徑用 `Path(fp).exists()` 直接檢查）`/resolved`（`library.resolve_video()` 的完整候選規則跑過一次的結果）；正常情況（真的沒紀錄、或有紀錄且能播）完全不受影響，不多回傳這個欄位。前端 `loadDay()` 在 `PL_CLIPS` 為空時，若回應帶 `debug`，用 `console.warn` 印出來，方便使用者直接複製瀏覽器主控台內容回報。**下一步**：需要使用者下次遇到同樣情況時，把 Console 印出的 `debug` 內容（尤其是 `stored_file_path` 和 `stored_path_exists`）貼回來，才能鎖定是路徑格式問題還是檔案真的不在了。 |
| 9.2 | 主播/日期/當天清單三個清單加篩選＋排序＋可調欄寬 | 三個清單都有搜尋框、點欄位標題排序、拖曳欄框調寬且重整後記得 | ✅ | 新增一個共用的輕量 mini-table 元件（`mtInit(cfg)`，純 vanilla JS，符合專案零建置限制，不引入任何套件）：**篩選**＝單一搜尋框，即時比對該列所有欄位的顯示文字（大小寫不分、substring match）；**排序**＝點欄位標題切換升冪/降冪（表頭顯示 ▲/▼），依欄位型別分「數字排序」（時長/場次/段數，比較原始數值而非格式化後字串，`1h02m` 不會被字串排序誤導）或「字串排序」（`localeCompare`，中文/日期字串皆適用）；**可調欄寬**＝欄位標題右緣有拖曳把手，`mousedown`→`mousemove`→`mouseup` 調整 `<colgroup><col>` 寬度，放開時存進 `localStorage`（key `pl_mt_widths:<storageKey>`），重整頁面/重開瀏覽器都會回復使用者調過的寬度。三個清單各自的欄位定義：**主播**＝主播/場次/時長；**日期**＝日期/段數/時長；**當天清單**＝時間/主播/檔名/時長/字幕（💬圖示）。原本各自獨立的 `renderAnchors()`/`renderClips()`（純字串樣板產生一排排 `<button>`）已整個移除、改用 `PL_MT.anchors/dates/clips` 三個 `mtInit()` 控制物件，資料變動時呼叫 `._render()` 重畫（不重建欄寬/篩選/排序狀態）；當前選中的主播/日期/片段用 `.is-current` 高亮（比對邏輯：主播比對 `anchor_name`、日期比對 `date`、片段用陣列參照 `indexOf(row)===PL_INDEX`，跟排序/篩選後的顯示順序無關，永遠對得到原始索引）；點列會呼叫對應的 `selectAnchor()`/`loadDay()`/`playClip()`，跟舊版按鈕點擊行為等價。容器寬度順手加寬（主播 170→220px、日期 170→200px、當天清單 280→340px）給表格欄位留空間，太窄時 `.mt-scroll{overflow:auto}` 會出現水平捲動，不會裁切內容。**驗證**：`index.html` 大括號配對 870/870、無重複 id、`html.parser` 標籤配對 0 錯誤、`node --check` 對抽出的 `<script>` 內容驗證 JS 語法通過；grep 確認 `renderAnchors`/`renderClips`/`pldate-`/`plclip-`/`planchor-` 這些舊版按鈕清單的殘留引用全部歸零（無孤兒呼叫）。**限制**：沙箱無瀏覽器，拖曳調欄寬、排序點擊、篩選輸入這些互動行為只能靠程式邏輯推導＋靜態檢查，無法截圖實測，需要使用者實際試用後回報。 |

---

## 階段 10 — 修復根因：分段錄製 file_path 存成 %03d 樣板，回放全部空清單（2026-08-26）

**背景**：使用者依階段9加的診斷欄位回報實際 `debug` 內容，真相大白：`stored_file_path` 是 `...三耳开开喵CatB__...\_2026-08-26_18-31-44_%03d.ts`——路徑裡直接帶著字面上的 `%03d`。這是 ffmpeg `-f segment` 分段輸出的 printf 樣板參數（main.py 的「分段錄製」功能，設定鍵 `分段錄製是否開啟`），ffmpeg 執行時會把 `%03d` 換成流水號（`_000.ts`／`_001.ts`…）分開寫檔，這個樣板字串本身**從來不是、也不會是**一個真實存在的檔案。main.py 在錄製結束呼叫 `_tracker.session_end(file_path=save_file_path, ...)` 時，把這個原始樣板路徑直接寫進 `recording_history.db`，導致往後只要開啟了分段錄製，該筆紀錄的 `file_path` 永遠對不到任何實體檔案——這正是「日期清單有段數、點進去卻空的」、且「全部都是這樣」的根本原因（只要有開分段錄製，每一筆都中招）。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 10.1 | main.py：錄製結束存檔路徑前，把 ffmpeg 樣板解析成真實片段路徑 | 分段錄製完成後 `recording_sessions.file_path` 存的是真實存在的檔案，不是 `%03d` 字面樣板 | ✅ | 新增 `_resolve_recorded_file_path(save_file_path)`（`main.py` 第346行附近，`converts_mp4`/`converts_m4a` 旁邊）：偵測路徑含 `%0?\d*d`（`%03d`／`%d`／`%04d` 等 printf 寬度樣板都認得），用 `glob.escape()` 把路徑裡「真的存在、剛好是 glob 特殊字元」的部分（`[`／`]`／`?`／`*`，例如主播名稱剛好帶方括號）當純文字看待，再把 `%03d` 換成萬用字元 `*` 去 `glob.glob()`，取編號最小（即最早、時間上第一段）的真實片段；沒有 `%` 的路徑原樣傳回（非分段錄製零影響，等於是個安全的直通函式）。**四個呼叫點**全部改用它包住 `save_file_path` 再傳給 `_tracker.session_end()`：`check_subprocess()` 的手動停止分支（原1442→現1442一帶）、正常完成分支、`only_flv_record` 直接下載分支的成功／例外兩處——四處都是同一顆函式呼叫，沒有另外寫特判邏輯，之後只要在別的地方新增 `session_end()` 呼叫，套用同一顆函式就會自動吃到這個修復。**驗證**：`py_compile` 通過；獨立寫測試腳本模擬「主播名稱含特殊 Unicode 組合字元」（複現使用者實際回報的 `˖ˋˏᰔᚚˎˊ˖` 這類字元）與「主播名稱含 `[VIP]` 方括號」兩種情境，各自在暫存目錄建立 3 個假分段檔（`_000.ts`/`_001.ts`/`_002.ts`），確認樣板路徑都能正確解析回 `_000` 那個真實檔案；`%` 不存在的一般路徑、以及片段檔案不存在的情況也各自測過（分別回傳原樣路徑、`None`，不會拋例外）。**這個修法在 main.py，屬於錄製器（exe）程式碼**——照專案慣例，要等 `build_main_exe.bat` 重建 exe＋`deploy.bat` 部署後，**下一次**新的分段錄製才會存對路徑；重建前已經在跑的 exe 不受影響，仍會繼續存錯（不會更糟，只是還沒修好）。 |
| 10.2 | library.py：`resolve_video()` 往回相容舊資料，不用等重建 exe / 不用資料庫遷移腳本 | 資料庫裡舊的 `%03d` 樣板路徑，只要磁碟上片段檔還在，回放頁重啟 web_ui 後立刻能撥放；沒有引入額外遷移腳本或改資料庫內容 | ✅ | 只改 main.py 只能救「以後新錄的」，使用者現在就想看「已經錄好」的內容才是眼前的痛點，而且改 DB 內容（寫一支遷移腳本改寫 `file_path` 欄位）風險較高、也違反「後端只讀不搶著改資料」的原則。改成在**讀取路徑**（`src/library.py` 的 `resolve_video()`，`/api/library/day`／`/api/library/dates` 等所有播放清單端點的唯一路徑解析點）加一段相容邏輯：偵測到 `stored_path` 含 `%0?\d*d` 樣板，就跟 main.py 那顆新函式一樣的邏輯（`glob.escape()` 保護特殊字元＋替換萬用字元＋`glob` 找最早的真實片段），不需要複製貼上——因為兩邊各自獨立、但演算法邏輯故意寫成完全對稱，之後如果要抽成共用函式（例如搬進 `src/common.py`）是可以做的後續重構，這次先求「兩邊都對」不求「零重複」（`main.py` 不能 import `src/library.py`，兩個檔案分屬 exe／web UI 兩個不同執行體，本來就不能共用一份程式碼，只能保持邏輯一致）。**維持 pure/IO 分離慣例**：`resolve_video()` 新增 `glob_fn` 參數（預設 `_default_glob`，內部包一層 try/except 呼叫真正的 `glob.glob`），呼叫端不用改，但單元測試可以照舊注入假的 `exists`/`glob_fn`，不必碰真實檔案系統。**驗證**：`py_compile` 通過；純函式單元測試（注入假 `exists`/`glob_fn`，不碰真實磁碟）涵蓋「樣板路徑找得到片段」「一般路徑不受影響（回歸測試）」「樣板路徑但片段不存在回傳 `None`、不拋例外」三種情況；另外用真實暫存目錄＋真實 `glob`/`Path.exists` 跑過一次同樣的 Unicode 主播名稱情境，確認預設參數（`web_ui.py` 兩處呼叫都用預設值，沒有額外改動）也能正確解析。**立即生效**：這是 `web_ui.py`／`src/library.py`，屬於 Web UI 執行體，只要重啟 web_ui（不需要重建 exe）就對舊資料生效。 |

**後續建議**（不在本次範圍內，供之後參考）：目前的相容邏輯只能解析「片段編號最小的第一段」，如果一場分段錄製有 3 段檔案，回放頁目前仍然只會播放「第一段」、不會自動接播第二三段（`recording_sessions` 資料表的設計是一列＝一個「場次」對一個 `file_path`，沒有記錄多檔案的關聯）。如果使用者常態開啟分段錄製、且希望回放能完整接續播完整場，需要更大的改動（例如替 `recording_sessions` 增加「同場次多檔案」的關聯，或回放頁的當天清單改成用檔案而非場次列出）——這是產品功能範圍的決策，先記錄在這裡，等使用者需要時再排入 ROADMAP。

### 10.3 追加修正：忘了考慮「.ts 已轉檔+刪除原檔」（同日回報，2026-08-26）

10.1/10.2 上線後，使用者拿另一位主播的實際 `debug` 資料回報：`resolved` 還是 `false`。原因是我漏想了一步——`resolve_video()`／`_resolve_recorded_file_path()` 把 `%03d` 換成萬用字元 `*` 時，**只換了片段編號，副檔名還是照抄資料庫存的原始副檔名（`.ts`）**；但這個安裝有開 `converts_to_mp4`＋`delete_origin_file`，segment 錄完會非同步轉檔成 `.mp4` 並刪掉原始 `.ts`，所以等使用者真的點開回放時，磁碟上早就只剩 `.mp4`、`.ts` 早就不存在了，glob 找 `*.ts` 自然找不到東西，跟真正的根因（分段樣板路徑）是兩個疊在一起的問題，10.1/10.2 只解了樣板部分。

**修法**：`resolve_video()` 的樣板分支改成先用 `Path(stored_path).with_suffix("")` 把副檔名也一併拿掉（不只換編號），再依照非樣板分支同一套優先序（`_hevc.mp4` 壓縮版優先 → 逐一嘗試 `.mp4/.mkv/.flv/.ts` → 各自的 `_hevc.<ext>`）組合出多組 glob pattern 依序嘗試，第一組有找到任何存在的檔案就回傳（sorted 取編號最小/最早）。`main.py` 的 `_resolve_recorded_file_path()` 比照同一邏輯（用 `rsplit(".", 1)` 拆副檔名＋依序嘗試 `[原副檔名, mp4, mkv, flv, ts]`），保持兩邊寫法對稱。**驗證**：重現使用者這次的實際場景——只留轉檔後的 `.mp4`（`.ts` 已刪），確認能正確解回 `.mp4`；再疊加已被 H.265 壓縮（`_hevc.mp4`）的情況，確認優先回傳壓縮版；`main.py` 那顆函式也用同樣情境測過。`py_compile` 皆通過。

---

## 階段 11 — 回放頁：主播/日期各自獨立篩選 + 彈幕欄位 + 排版重排 + 主播分組修正（2026-08-26）

**背景**：使用者一次提出四件事——(1) 主播欄位把同一個真人主播拆成好幾筆（例如「序号9 猫饼mako」「序号12 猫饼mako」被當成兩個不同主播）；(2) 希望日期／主播可以任選一個當篩選條件，不是強制先選主播；(3) 當天清單要標示這段有沒有彈幕；(4) 排版——當天清單移到日期下方（同一欄堆疊），同步彈幕欄移到播放器右邊、跟播放器同一行不換行。另外使用者回報回放頁點開後「完全沒有載入彈幕」。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 11.1 | 主播分組錯誤根因修復：main.py 的「序号N」前綴 | 同一真人主播不論序號怎麼變動，都歸在同一個「主播」項目下 | ✅ | **根因**：`main.py` 第1184行 `record_name = f'序号{count_variable} {anchor_name}'`——這是上游專案原本的設計，`count_variable` 是這個網址在 `URL_config.ini` 裡的行號，純粹是為了主控台能分辨不同執行緒印出的訊息，跟主播本身無關；但這個帶編號的字串被整條存進 `recording_sessions.anchor_name`，行號只要因為使用者在設定檔裡增刪別的網址而跟著往前後移，同一個真人主播往後錄的紀錄就會被系統當成不同主播處理。這正是 `list_anchors()`／`build_playlist()` 用原始 `anchor_name` 分組時，會把同一個人拆成好幾筆的原因。**修法**：`src/library.py` 新增 `canonical_anchor_name(name)`，用正則 `^序[号號]\d+\s+` 把這個前綴去掉；`list_anchors()` 改用它當分組 key 也當顯示值，`build_playlist()` 的每個 clip 的 `anchor_name` 也改成這個乾淨版本；`web_ui.py` 的 `_sessions_for_anchor()` 比對篩選條件時，也把資料庫存的原始 `anchor_name` 用同一顆函式轉過再比對（不然篩選會比對不到，因為前端拿到的是已經去過前綴的乾淨名稱）。**驗證**：純函式單元測試——`canonical_anchor_name("序号9 猫饼mako")` → `"猫饼mako"`；建構兩筆「序号9 猫饼mako」「序号12 猫饼mako」的假紀錄丟進 `list_anchors()`，確認合併成一筆（`count=2`）而不是兩筆；`py_compile` 通過。**只影響回放頁的顯示/分組/篩選**，沒有改資料庫內容、沒有改 `main.py`（這次問題出在讀取端如何解讀既有資料，不需要動錄製器、不需要重建 exe，重啟 web_ui 就對舊資料生效）。 |
| 11.2 | 主播／日期改成各自獨立、可任選的篩選條件 | 一進頁面就看得到全部日期清單（不用先選主播）；點主播＝加篩選，再點同一位＝取消篩選，日期照樣可以獨立點 | ✅ | 拿掉「一定要先選主播才能選日期」的強制順序（滿足使用者「需要可以選擇根據日期或是主播篩選」）。後端 `/api/library/dates`／`/api/library/day` 的 `anchor` 參數本來就是可選的（階段8做的），不用再改；純前端調整：`initPlayer()` 一開始就抓 `/api/library/anchors` 填主播清單，同時呼叫新的 `_plReloadDates(false)`（不帶 anchor 篩選）抓「全部日期」並自動選最新一天播放；`selectAnchor(name)` 從「設定主播」改成「切換主播篩選」——`PL_ANCHOR = (PL_ANCHOR === name) ? null : name`，點同一位主播兩次＝清除篩選、變回看全部主播；換篩選後呼叫 `_plReloadDates(true)`，如果原本選的日期在新篩選結果裡還存在就保留（例如篩到某主播、原本那天他剛好也有錄），不存在才退回最新一天。`pl-day-label` 有篩選中的主播時會多顯示「（主播名）」提示目前套用的篩選範圍。**驗證**：`index.html` 大括號配對 872/872、無重複 id、`html.parser` 標籤配對 0 錯誤、`node --check` 通過；grep 確認新函式 `_plReloadDates`／改過的 `selectAnchor`／`initPlayer` 都有定義且無孤兒呼叫。 |
| 11.3 | 當天清單新增「彈幕」欄位（有沒有彈幕紀錄） | 每個 clip 列出是否有彈幕，跟既有「字幕」欄位分開、不共用同一個圖示 | ✅ | 新增 `web_ui.py._clip_has_danmaku(item)`：用該 clip 的 `start_time`/`duration_sec` 算出時間窗，優先用 `live_url` 查 `danmaku_store.count()`（跟 `/api/library/clip/<id>/danmaku` 同一套「優先比對 url、沒有才退回比對 anchor」邏輯一致），任何例外都當作「沒有」而不是讓整個清單 500（跟其餘彈幕功能一樣是 best-effort）。`build_playlist()` 順便補上 `live_url` 欄位（原本只有 `anchor_name/platform/start_time/duration_sec/path/filename/has_srt/has_ass`，缺這個查彈幕要用的欄位）；`/api/library/day` 算完 `has_danmaku` 後把 `path`／`live_url` 這兩個內部用的欄位從回給前端的資料裡拿掉（沿用原本 `path` 就會被拿掉的慣例）。前端 mini-table 新增「彈幕」欄（💬圖示），並把原本「字幕」欄的圖示從 💬 改成 📝，避免兩欄看起來一樣分不清楚代表什麼。**驗證**：`py_compile` 通過；`index.html` 靜態檢查同 11.2。 |
| 11.4 | 排版重排：當天清單移到日期下方、彈幕欄移到播放器右邊不換行 | 日期清單和當天清單堆疊在同一欄；播放器和彈幕欄同一行，視窗變窄時彈幕不會被擠到下面另起一行 | ✅ | HTML 結構調整：原本「主播／日期／播放器／當天清單／彈幕」五欄並排，改成三個區塊——① 主播（獨立一欄，200px）；② 日期＋當天清單合併成一欄（260px，內部 `flex-direction:column`，日期在上、當天清單在下，這就是「清單移到日期下方」）；③ 播放器＋同步彈幕包成一個 `flex:1; display:flex; flex-wrap:nowrap` 的子容器（這就是「不換行」——只限制播放器和彈幕這一組不會被拆開到不同行，最外層大容器仍保留 `flex-wrap:wrap`，視窗真的太窄時是整組「播放器+彈幕」一起換到下一行，而不是彈幕自己被擠掉）。`togglePlayerDanmaku()`／`pl-dm-*` 系列既有函式與 id 都沒變動，只是外層包裝變了，收合/展開彈幕欄的行為不受影響。**驗證**：`index.html` 靜態檢查同 11.2；人工檢查新版 HTML 巢狀結構 flex 屬性符合預期（外層 wrap、內層播放器+彈幕 nowrap）。 |

**尚待使用者確認**：「完全沒有載入彈幕」這個回報，目前判斷最可能的原因是階段10修好之前，`_resolve_clip_session()`（`/api/library/clip/<id>/danmaku` 用來把 clip id 換回真實檔案的函式）內部也是呼叫同一顆 `resolve_video()`，所以在分段錄製的樣板路徑修好之前，同步彈幕本來就會因為連對應的 clip session 都找不到而整段抓不到彈幕——理論上階段10的修復會連帶讓這個功能恢復正常。如果重新整理、重新選片段後彈幕還是空的，`loadPlayerDanmaku()` 現在會在查無彈幕時把查詢用的時間窗印到瀏覽器 Console（`[回放] 這段查無彈幕，查詢窗：...`），麻煩使用者到「彈幕查詢」頁面用同樣的主播／時間範圍手動查一次交叉比對，才能判斷是「這段真的沒有彈幕紀錄」還是比對邏輯還有別的問題。

---

## 階段 12 — 回放支援分段錄製多檔案播放清單（2026-08-26）

**背景**：使用者實測後回報「時間長度對不上、也只有一段」——日期清單顯示某天「1 段、5h40m」，但點進去當天清單只有一列，實際播放的影片卻只有 30 分鐘。根因：main.py 開了「分段錄製」（`视频分段时间(秒)`，預設 1800 秒／30 分）時，一場 5h40m 的直播會被 ffmpeg 切成約 11 個實體檔案，但 `recording_sessions` 資料庫**一場直播只有一列**（`duration_sec` 存的是整場直播的總時長）。階段10只修了「把 `%03d` 樣板解回*第一個*真實片段」，所以回放永遠只給得出這場直播最前面 30 分鐘，卻在日期清單顯示整場 5h40m 的總時長、段數還寫「1」——這正是使用者看到的落差。

| # | 任務 | 驗收 | 狀態 | 備註 |
|---|------|------|------|------|
| 12.1 | `library.py` 新增 `resolve_all_segments()`：找出一個 session 對應的*所有*真實片段檔案 | 分段錄製的 session 能列出磁碟上實際存在的每一個片段（不只第一個），同編號有壓縮版跟原始版時只留優先序較高的一份 | ✅ | 邏輯跟 `resolve_video()` 的樣板分支同一套優先序（壓縮 `_hevc.mp4` 優先→依序嘗試 `.mp4/.mkv/.flv/.ts`），差別是**收集每個編號**而不是「找到第一個就回傳」。額外建一個對照用的 regex（`re.escape(stem)` 把樣板中的 `%03d` 換成 `(\d+)` capture group），拿磁碟上 glob 出來的真實檔名反推它是第幾段——注意 `re.sub()` 的替換字串如果直接寫 `r"(\d+)"` 會被誤判成合法性有問題的 regex escape（`\d` 不是 `re.sub` 替換字串認得的跳脫序列，會丟 `bad escape \d`），改用 lambda 回傳字面字串才不會被二次解讀。 |
| 12.2 | `build_playlist()`／`group_by_date()` 改用多片段：一個 session 展開成多列，每列各自估算起訖時間 | 當天清單的段數/時長跟日期清單的段數互相對得起來，各段的起始時間依序遞增、時長合理（最後一段可能比較短） | ✅ | 新增共用的 `estimate_segment_window(start_iso, total_duration_sec, index, segment_seconds)` 純函式（`build_playlist()` 和 web_ui.py 的 clip 解析都呼叫**同一顆**，避免兩邊各寫一份估算邏輯，之後改分段規則只要改一個地方）：第 idx 段的起始時間＝`session 起始 + idx*segment_seconds`，時長＝`min(segment_seconds, 剩餘總時長)`（最後一段自然比較短，例如 5h40m／30min一段會有 11 個滿 30 分的段 + 1 個 10 分的尾段）。`build_playlist()` 偵測到 `resolve_all_segments()` 有結果就展開成多列（每列各自查字幕 sidecar，因為字幕檔是跟著各自的真實檔名走的）；`group_by_date()` 的 `count` 也同步改成算真實片段數而不是 DB 列數，這樣「日期清單的段數」跟「點進去當天清單看到幾列」才會一致——這是這次修復很重要的一環：**只修 build_playlist 而不修 group_by_date，使用者還是會看到「日期寫 1 段、點進去卻有 11 列」的新落差**，等於把 bug 從一個地方搬到另一個地方。是估算不是精算（真實片段的關鍵幀切割點可能跟 `segment_seconds` 的整數倍有一點點誤差），但排序/分段瀏覽用途已經足夠準確。 |
| 12.3 | `web_ui.py`：`/api/library/day` 帶入實際的分段秒數設定；`_resolve_clip_session()` 也要認得多片段，且回傳「這一段」而非整場的起訖時間 | 點當天清單裡的任何一段（不只第一段）都能正確播放；每一段的同步彈幕時間窗對到該段自己的時間範圍，不會整段拿整場 5h40m 當窗口 | ✅ | 新增 `get_segment_seconds()`（讀 `config.ini` 的 `视频分段时间(秒)`，格式同既有 `get_folder_rules()` 的正則讀法，預設 1800 對齊 main.py 的預設值），`/api/library/day` 呼叫 `build_playlist(..., segment_seconds=get_segment_seconds())`。**這步是必要的，之前只改 library.py 是不夠的**：`_resolve_clip_session()`（`/api/library/video/<id>` 和 `/api/library/clip/<id>/danmaku` 都靠它把 clip id 換回真實檔案）原本只呼叫單一片段版的 `resolve_video()`，只認得清單裡的第一段——階段11新增的多片段清單雖然列出了全部片段，但點第 2、3...段一樣會播放失敗／404，因為 `_resolve_clip_session` 根本找不到那些 clip id。改成先呼叫 `resolve_all_segments()`，找到符合的片段編號後，用同一顆 `estimate_segment_window()` 算出「這一段」的 start_time/duration_sec 蓋掉 session 原本的值再回傳——這樣同步彈幕的查詢時間窗才會對到正在播放的那 30 分鐘，而不是整場直播的 5h40m（時間窗抓錯會讓彈幕要嘛跟畫面對不上、要嘛整批漏接）。**驗證**：`py_compile` 全過；純函式單元測試涵蓋——`group_by_date`／`build_playlist` 用假的 4 個片段檔重現使用者的實際案例（5h40m 總長、4 段已存在的檔案），確認段數=4、時長遞增正確（09:52→10:22→10:52→11:22，每段30分）；`estimate_segment_window` 對最後一段（第 11 段，理論上第 12 個 30 分區間）算出時長=600秒（10分尾段）；`resolve_all_segments` 直接測回傳的編號清單；模擬 `_resolve_clip_session` 對第 3 段（idx=2）的邏輯，確認回傳的 start_time 是該段自己的（10:52:15）而非整場的（09:52:15）；另外重跑一次非分段錄製的一般情境（regression），確認單一檔案 session 行為完全不變。 |

**與「並且沒有彈幕」的關係**：使用者同時回報這段（分段錄製的第一段，00:00-30:00）沒有彈幕。這次的修復讓每段的彈幕查詢時間窗變得更精準（30分鐘窗口，之前如果連 session 都解析不到，等於完全查不到），理論上不會讓彈幕變得更難找到；但沒辦法排除「這段真的沒有錄到彈幕」的可能性（danmaku 擷取是 best-effort，見 AGENT.md 已知陷阱）。同階段11備註的建議一樣：麻煩用瀏覽器 Console 印出的查詢時間窗，或「彈幕查詢」頁面手動交叉比對，才能進一步判斷。

---

## 交付與驗證慣例（每階段結束必做）
1. 後端：複製到暫存 `py_compile`（避免掛載截斷誤報）；純邏輯附/更新單元測試。
2. 前端：檢查新增 element id 各出現一次、JS 參照函式皆定義、無殘留舊 id。
3. 手動走過該階段主要流程（沿用現有 API，行為不得退化）。
4. 更新本表狀態欄 + 進度總覽 + Cowork 任務清單。

---

## 全表最終狀態（整個 UI/UX 重構專案，2026-08-26）

| 階段 | 名稱 | 狀態 | 完成度 |
|------|------|------|--------|
| 0 | 骨架（tokens + 元件 + Alpine + 路由殼） | ✅ | 4/4 |
| 1 | 遷移現有頁到新框架 | ✅ | 5/5 |
| 2 | 儀表板 panel 化 + 全域狀態 | ✅ | 4/4 |
| 3 | 回放同步彈幕 | ✅ | 3/3 |
| 4 | 新頁（P1-1 錄製設定 / P1-2 統計）+ 壓縮占比 | ✅ | 4/4 |
| 5 | 技術債清理 | ✅ | 4/4 |
| 6 | 破版修復：全域 CSS 洩漏 + 統計頁載入失敗 | ✅ | 2/2 |
| 7 | 側邊欄排版重寫（icon/label 分離） | ✅ | 1/1 |
| 8 | 回放頁改「先選主播、再選日期」 | ✅ | 1/1 |
| 9 | 回放頁清單加篩選/排序/可調欄寬 + 放不出影片的診斷欄位 | ✅ | 2/2 |
| 10 | 修復根因：分段錄製 file_path 存成 %03d 樣板，回放全部空清單 | ✅ | 2/2 |
| 11 | 回放頁：主播/日期各自獨立篩選 + 彈幕欄位 + 排版重排 + 主播分組修正 | ✅ | 4/4 |
| 12 | 回放支援分段錄製多檔案播放清單 | ✅ | 3/3 |

**全專案 22/22 項全部完成。** 五個階段皆為單一 DOM／等價或擴充重構，未破壞既有功能；上游檔案（spider/stream/room/utils/ab_sign/proxy/logger/initializer）全程未被觸碰；零新增第三方依賴，前端維持零建置。技術債清理階段額外處理了一個 stage4 遺留的孤兒 import（`web_ui.py` 的 `configparser`）與一個 stage4 才出現、跟舊三檔案同款的 `timedelta` 格式化重複（`/api/stats/export`），一併納入 5.1/5.2 的範圍。桌面 widget（`widget.py`/`widget_window.py`/`web_static/widget.html`/`start_widget.bat`）已標記棄用但保留檔案，是否刪除留給使用者決定；`install_autostart.bat` 確認未安裝 widget 捷徑（此前已是既有行為）。剩餘的產品待辦（P1-3 首次設定精靈、P2 系列）記錄在 `ROADMAP.md`，不在本次 UI/UX 重構範圍內。
