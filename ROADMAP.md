# 產品改進路線圖 — DouyinLiveRecorder（自訂版）

> 以 PM 角度整理。目標使用者：**個人自用**（技術背景、常駐錄製、願意看 log）。
> 因此優先級偏向「**不要靜默失敗**」與「**少手動維護**」，而非廣泛推廣或多人協作。
> 最後更新：2026-06-12

---

## 1. 產品定位

這是一個**常駐型、單機、個人用**的多平台直播錄製工具。核心價值有三層：

1. **能錄到** — 60+ 平台的直播抓取（上游 spider.py，自動更新）
2. **錄得有紀錄** — 自訂的時長追蹤層（SQLite），可查單一主播 / 每日 / 累計
3. **管得動** — Web 控制台（側邊欄導覽，含設定/統計頁）+ 每位主播的錄製上限（桌面 widget 已於 2026-08 UI 重構棄用，功能全併入瀏覽器控制台）

它**不是** SaaS、不是多人協作工具、也不面向非技術使用者。路線圖的取捨都以此為前提。

---

## 2. 現況快照

**強項**：平台覆蓋廣、時長追蹤設計乾淨、Web 控制台功能完整、spider.py 自動更新、健康警報、磁碟自動管理、**錄完自動壓縮（多編碼器並行省空間）**、特別關注（每分鐘檢測開播）。

**主要缺口**（依對使用者的痛感排序）：

| # | 缺口 | 痛感 | 根因 |
|---|------|------|------|
| 1 | 故障靜默 — 錄製器掛掉/cookie 過期/磁碟滿，你不會知道 | 高 | 只有開播/關播推播，無健康推播 |
| 2 | 錄壞了不知道 — 0 byte、中斷、損毀檔無人察覺 | 高 | 錄完只看 ffmpeg returncode，不驗檔 |
| 3 | Cookie 過期需手動重抓，過期後該平台靜默失敗 | 高 | 無 cookie 健康偵測、無提示 |
| 4 | ~~磁碟會被錄到滿~~ ✅ 已解（滿前停錄＋選用清理＋錄完自動壓縮省空間） | — | — |
| 5 | ~~設定要手改 .ini（中文鍵、是/否）~~ ✅ 已解（控制台「設定 → 錄製設定」頁） | — | — |
| 6 | ~~已有的好東西藏起來了（query_duration CLI）~~ ✅ 已解（控制台「統計」頁） | — | — |
| 7 | port 8765 寫死，被占用就起不來 | 中 | 無 port fallback |
| 8 | 全中文，`language=en` 無效 | 低（自用） | UI 字串硬編碼 |
| 9 | 無自動化測試 | 低（自用） | 無 test suite |
| 10 | main.py 2200 行、平台邏輯內聯 | 低（自用） | 無平台抽象層 |

---

## 3. 優先級框架

用「**影響 × 工作量**」二維取捨。自用情境下，**影響＝減少你被迫手動介入的次數**。

```
高影響 │  P0：健康警報(已完成)        │  P1：設定 GUI(已完成)
       │      錄製完整性驗證           │      歷史統計頁(已完成)
       │      Cookie 過期偵測          │      首次設定精靈
       │      磁碟自動管理             │
       │  ── 先做這格 ──              │
       │                              │
低影響 │  Port fallback               │  英文 i18n
       │  log 保留歷史                │  遠端存取
       │  (順手做)                    │  自動化測試 / 重構
       └──────────────────────────────┴──────────────────────────
         低工作量                       高工作量
```

原則：**先清左上格**（高影響低工作量），那裡每一項都直接減少你半夜被叫起來的機率。

---

## 4. 路線圖

### ✅ 已完成（本次）— 健康警報系統

把產品從「壞了你自己發現」變成「壞了它通知你」。

- `src/health.py`：磁碟 / 錄製器行程 / 資料庫三項健康評估 → ok / warn / critical
- `src/alerts.py`：複用既有推播管道（ntfy / Telegram / Webhook），含防洗版節流與恢復通知
- `web_ui.py`：60 秒 watchdog 背景緒 + `/api/health`、`/api/alerts` 端點
- 控制台：健康狀態膠囊 + 通知設定面板（含一鍵測試）

**成功指標**：磁碟快滿 / 錄製器掛掉 / DB 異常，能在 1 分鐘內推到手機。

---

### ✅ P0 — 已完成（穩定性批次）

全部餵進健康/警報系統，邊際成本低、回報高。已實作並通過單元測試。

**✅ P0-1　錄製完整性驗證** — `src/health.py: classify_recording / evaluate_recordings`
- 掃描近 24h 完成的錄製，依檔案大小（<64KB）、時長（<15s）、`finished_reason=error`、截斷檔判定為 suspect，標紅並可推播。控制台健康膠囊會顯示「偵測到可能損壞的錄製」。
- **（2026-06 修正）已歸檔判定**：錄很久又正常結束、只是檔案事後被移走/上傳網盤刪掉的，不再誤判為損壞——先前把「檔案不存在」一律當損壞造成大量誤報，現改為只有「秒退＋無檔」「檔案還在卻被截斷」「error/crash」才標記。

**✅ P0-2　Cookie 過期偵測** — `src/health.py: looks_like_cookie_issue / evaluate_cookie_health`
- 啟發式辨識認證/登入類錯誤（401/403/登录/cookie/實名…），指名是哪個平台，提示去 config.ini 更新 cookie。

**✅ P0-3　磁碟自動管理** — `src/disk_manager.py` + `/api/disk/policy`、`/api/disk/cleanup`
- 兩段式：（a）剩餘 < 暫停門檻時，規劃暫停新錄製（含 hysteresis 防抖）；（b）選用的「自動刪最舊、保留最近 N 天、保護錄製中檔案」清理（**預設關閉**，控制台可預覽再執行）。磁碟不會再被錄到 100%。

**✅ P0-4　Port 衝突 fallback** — `src/netutil.py`
- 8765 被占用就自動試 8766–8770，實際 port 寫進 `config/webui_port.txt`（控制台 topbar 會顯示目前 port；widget 已棄用不再依賴這個檔案）。

**✅ P0-5　log 保留歷史** — `web_ui.py: _setup_console_logfile`
- web_ui 主控台輸出 tee 到 `logs/web_ui.log` 並輪替保留近 5 份，過去的故障不再因重啟消失。

---

### ✅ 已完成（2026-06 批次）— 省空間 + 快抓開播 + 修補

延續 P0「少手動維護」的方向，補上儲存與時效兩個高頻痛點，並修掉兩個回報的 bug。

**✅ 錄完自動壓縮（省空間）** — `src/compressor.py` + `/api/compress`、`/api/compress/scan`、`/api/compress/pause`
- 背景每 10 分鐘掃描保存夾，把**已錄完**（15 分鐘沒再寫入）的影片轉 H.265，體積約省 40–60%、畫質幾乎不變。
- **安全**：先輸出暫存檔，ffmpeg 成功＋時長與原檔一致（±max(2s,1%)）＋確實變小三項全過才刪原檔；錄製中檔案絕不碰。
- **多編碼器並行**：自動偵測 `hevc_nvenc`（NVIDIA）/ `hevc_qsv`（Intel）/ `hevc_amf`（AMD）/ `libx265`（CPU），每個編碼器一條 lane 從同一佇列各取不同檔同時壓，吞吐量倍增；可自動或手動勾選 lane，硬體 lane 失敗自動退回 CPU。控制台預設用 GPU。
- **可暫停**：面板「暫停壓縮」立即中止進行中的壓縮，原檔保留、不標記失敗，恢復後重試。

**✅ 特別關注（每分鐘檢測開播）** — `main.py: is_priority_url()` + `web_ui.py` + `/api/streamers/priority`
- 一般主播依 config.ini 循环时间（預設 300s）輪詢；標為特別關注的主播偵測間隔縮短為 60 秒，第一時間抓到開播。主播清單「關注」欄 ★/☆ 一鍵切換，旗標存 `streamer_limits.json` 的 `priority`。
- ⚠️ 輪詢邏輯在 `main.py`（打包進 exe），**改動需 `build_main_exe.bat` 重建 exe** 才生效；URL 比對用正規化（`is_priority_url`）避免 scheme/query 差異導致靜默失效。

**✅ 修：「自動恢復(分)」編輯留空清不掉** — `web_ui.py: set_limit` + 前端 saveModal
- 舊版前端把留空送成 `null`、後端當「不變更」導致清不掉；現改為留空/0 = 直接清除（恢復隔天 00:00 重置）。

---

### ✅ 已完成（2026-08 批次）— 彈幕 + 回放 + Discord 通知

把「錄下來的內容」變得可查、可看、可通知。

**✅ 彈幕擷取 + 查詢** — `src/danmaku_store.py`（純邏輯）、`src/danmaku_capture.py`（best-effort）+ `/api/danmaku*` + 控制台「彈幕查詢」分頁 + `query_danmaku.py`
- 擷取：web_ui 背景監看正在錄的抖音主播，連 IM websocket 擷取彈幕入 `config/danmaku.db`（純標準庫、不動 recorder、不需重打包）。查詢：依主播/關鍵字/發言者/時間，分頁、CSV 匯出。
- ⚠️ 擷取層 best-effort（抖音協定會改版、無法對真實抖音驗證）；查詢/儲存/UI/CLI 完全獨立可靠。

**✅ 彈幕轉字幕** — `src/danmaku_subtitle.py`（純邏輯）+ `/api/danmaku/subtitle` + `query_danmaku.py --subtitle`
- 彈幕時間戳 − 錄影開始時間 = 影片中出現秒數，產出旁載 `.danmaku.ass`（滾動彈幕）+ `.danmaku.srt`（底部字幕），不動原影片。

**✅ 錄影回放（依日期連續播放）** — `src/library.py`（純邏輯）+ `/api/library/*` + 控制台「錄影回放」分頁
- 依日期分組，選一天連續播放當天所有片段、自動接下一段。HTTP Range 串流可拖曳；SRT 即時轉 WebVTT 供 `<video>` 字幕。檔案解析自動對應壓縮後 `_hevc.mp4` / `.ts→.mp4`；clip id 用 SHA1 + 目錄越界防護。**取代原 P1-4「錄製檔瀏覽器」**。

**✅ Discord 錄製通知** — `src/rec_notify.py` + `/api/rec_notify*` + 設定頁「錄製通知（Discord）」
- 主播開始/結束錄製時發 Discord embed 卡片（含主播/平台/開始時間/時長/時間戳）。同一輪多位主播**合併成一張卡片**避免洗版；由 web_ui 60s 輪詢驅動、不需重打包。可設 proxy、帶 UA 解 Cloudflare 1010、測試按鈕顯示真正錯誤、可「傳送目前狀態」。

---

### ✅ 工具/部署改進（隨手完成）
- `start_console.bat`：輕量啟動（recorder + web_ui + 自動開瀏覽器，跳過會退出的 widget），**日常建議用這支**。
- `deploy.bat`：一鍵部署新 exe（停舊 → 備份 config → 覆蓋 → 重啟；純 ASCII 避免 .bat 編碼問題）。
- `start_widget.bat`：加自動開瀏覽器、讀實際 port。**已棄用**（見下方 UI 重構），保留給仍想用舊浮動視窗的人手動執行。

---

### ✅ 已完成（2026-08 批次）— 控制台 UI/UX 重構（含 P1-1、P1-2）

把控制台從「兩個分頁 + 彈出式通知設定面板」重做成側邊欄導覽的完整主控台，分五階段完成（規格見 `UIUX_SPEC.md`，逐項驗收見 `UIUX_TASKS.md`），全程**等價/擴充重構、未破壞既有功能**：

- **階段 0-1（骨架 + 頁面遷移）**：design tokens、共用元件 class、側邊欄（監控/內容/設定三組）+ topbar + hash 路由，舊有 4 頁內容整段搬進新框架（單一 DOM，不留重複 id）。
- **階段 2（儀表板 panel 化 + 全域狀態）**：儀表板拆成 KPI / 系統健康 / 磁碟 / 正在錄製 / 壓縮佇列多個 panel；topbar 健康燈 + port + 刷新、側邊欄底部錄製器/磁碟迷你狀態全站共用同一份輪詢，不多打 API。
- **階段 3（回放同步彈幕）**：`GET /api/library/clip/<id>/danmaku` 新端點；回放頁新增第 4 欄彈幕列表，跟播捲動 + 目前行高亮 + 點擊跳轉 + 關鍵字搜尋 + 可收合，與既有 VTT 字幕並存。
- **階段 4（新頁 + 壓縮占比，即 P1-1／P1-2）**：
  - ✅ **P1-1 設定 GUI** — `GET/PUT /api/config` + 控制台「設定 → 錄製設定」頁：畫質/保存路徑/分資料夾/格式/循環/分段/Cookie 全可在網頁改，用逐行 regex 替換寫回、**保留 config.ini 原有註解與排版**，不用 configparser（避免整檔重新序列化弄丟格式）。
  - ✅ **P1-2 統計頁** — `GET /api/stats/overview|anchor`、`GET /api/stats/export` + 控制台「統計」頁：排行榜、摘要、單主播明細、CSV 匯出，直接對照 `query_duration.py` 的 SQL 邏輯。
  - 額外：`GET /api/compress/overview`（60s 快取）+ 壓縮占比 panel + 獨立於自動開關的「立即開始壓縮」手動觸發。
- **階段 5（技術債清理）**：新增 `src/common.py` 收斂 `alerts.py`/`compressor.py`/`rec_notify.py` 三份重複的 `deep_merge`/JSON 設定讀寫/`http_post_json`，以及 `duration_tracker.py`/`query_duration.py`/`web_ui.py` 重複的秒數→`H:MM:SS`格式化；移除確認零呼叫端的死碼（`rec_notify.py` 的 `_now_hm`/單則 `format_start`/`format_stop`/`build_start_embed`/`build_stop_embed`、`health.py` 的 `diff_issue_codes`）與多個未用 import；桌面 widget 全面標記棄用（README 註明、`install_autostart.bat` 確認不再安裝）。

**成功指標**：常用設定與時長統計 0 次需要手改 .ini 或開終端機；技術債清理後 `alerts.py`/`compressor.py`/`rec_notify.py` 不再各自維護一份 `deep_merge`/JSON 讀寫邏輯。

---

### 🟠 P1 — 中期（易用性，估 3–7 天/項）

讓日常操作不必碰 .ini、不必記 CLI 指令。

**~~P1-1　設定 GUI（在 Web UI 編輯 config.ini）~~（✅ 已完成，見上方「控制台 UI/UX 重構」）**

**~~P1-2　歷史 / 統計頁（把 query_duration 搬進 UI）~~（✅ 已完成，見上方「控制台 UI/UX 重構」）**

**P1-3　首次設定精靈**
- 問題：新環境要手動建 config、抓 cookie、填 URL。
- 提案：偵測到無設定時，引導式 4 步：保存路徑 → 加第一個主播 → （選）cookie → （選）警報通知。
- 影響：中　｜　工作量：中

**~~P1-4　錄製檔瀏覽器~~（✅ 已由 2026-08「錄影回放」取代）**
- 「錄影回放」分頁已能依日期瀏覽/播放錄影；若還想要「每主播分組 + 大小 + 開資料夾」的檔案清單視圖可再擴充，但核心需求已滿足。

---

### 🟢 P2 — 長期（擴展，視需要）

自用情境下優先度最低；若哪天想公開分享或多機使用再啟動。

- **英文 i18n** — 抽出 UI 字串、真正讓 `language=en` 生效。公開發布前必做，自用可緩。
- **遠端存取** — web UI 綁 0.0.0.0 + 簡單驗證 + （選）反向代理，讓你在外面也能看。需先補驗證，否則別開。
- **自動化測試** — 已先為 `health.py` 寫了單元測試；可延伸到 web_ui 的設定讀寫與限制器邏輯。
- **main.py 重構 / 平台抽象層** — 2200 行單檔、平台邏輯內聯，擴充新平台成本高。純技術債，使用者無感，最後再碰。

---

## 5. 明確不做（Non-Goals）

定義邊界跟定義要做的事一樣重要，避免範圍蔓延：

- ❌ **多使用者 / 權限系統** — 個人自用，不需要。
- ❌ **雲端 / 帳號 / 同步** — 維持單機、零後端依賴。
- ❌ **內建播放器 / 剪輯** — 交給既有的播放器與工具，不重造輪子。
- ❌ **追求「支援更多平台」** — 平台覆蓋已足夠，且 spider.py 由上游自動更新；穩定性 > 廣度。
- ❌ **商業化 / 打包成品** — 不是產品化目標。

---

## 6. 近期建議順序

P0（穩定性）、2026-06（省空間 + 快抓開播）、2026-08（彈幕 + 回放 + Discord 通知）、2026-08（控制台 UI/UX 重構，含 P1-1 設定 GUI、P1-2 統計頁）批次**均已完成**。剩餘：

1. **P1-3 首次設定精靈** — 新環境引導式設定。
2. P2（英文 i18n、遠端存取、正式 test suite、main.py 重構）維持「需要時再做」。
3. （擴展）彈幕擷取層若失效需跟進抖音協定；可考慮把彈幕字幕的 ASS 樣式做成面板可調。
4. （待決）桌面 widget（`widget.py`/`widget_window.py`/`web_static/widget.html`/`start_widget.bat`）功能已完全被瀏覽器控制台取代，目前僅標記棄用未刪檔；是否直接刪除留給使用者決定。

> 路線圖是活文件。每完成一項就把它移到「已完成」，並依實際使用痛感重排剩餘項目。
