# UI/UX 規格 — DouyinLiveRecorder 控制台重設計

> 狀態：**規劃中（尚未實作）**。本檔是重設計的權威依據，實作前以此為準。
> 目標：把「功能長很快、資訊架構沒跟上」的現狀，整理成符合後台標準規格的介面。
> 相關：`AGENT.md`（工程慣例）、`README.md`（使用說明）、`ROADMAP.md`（P1-1/P1-2 在此一併實作）。

---

## 0. 已確認的需求（決策紀錄）

| 項目 | 決定 |
|------|------|
| 範圍 | 資訊架構重整為主 |
| 導覽 | 左側邊欄，三大組（監控 / 內容 / 設定） |
| 技術 | Alpine.js（本地一份、無建置）+ design tokens + 元件化 CSS/JS，維持零建置雙擊即用 |
| 視覺 | 精緻現代暗色系（三層背景、語意狀態色、4px 網格 token） |
| 設定 | 從儀表板彈出面板 → 獨立「設定」區 5 子頁（錄製/通知/壓縮/磁碟/系統） |
| 全域 | 頂列健康燈＋port＋刷新；側邊欄底常駐迷你狀態；responsive 漢堡 |
| 新頁 | P1-1 錄製設定 GUI、P1-2 統計頁 **一併實作** |
| 儀表板 | **panel 化**，一眼掌握全局（不只數字卡） |
| 回放 | **雙欄：播放器 + 右側同步彈幕**（跟播捲動 + 可點跳轉 + 搜尋 + 可開關 + 時間戳 + 發言者） |

---

## 1. 資訊架構（側邊欄選單樹）

```
DouyinLiveRecorder                    ← 品牌區（logo + 標題）
────────────────────────────
監控 MONITOR
  📊 儀表板        #dashboard
  🎬 主播管理       #streamers
內容 CONTENT
  💬 彈幕          #danmaku
  ▶️ 回放          #player
  📈 統計          #stats        (P1-2 新)
設定 SETTINGS
  🎥 錄製          #settings/recording   (P1-1 新)
  🔔 通知          #settings/notify
  🗜️ 壓縮          #settings/compress
  💾 磁碟          #settings/disk
  🛠️ 系統          #settings/system
────────────────────────────
● 錄製器  ● 磁碟 45GB           ← 側邊欄底：常駐迷你狀態
```

**路由**：用 URL hash（`#dashboard`、`#settings/notify`…），Alpine 依 hash 切換 view。好處：可加書籤、重整不掉頁、瀏覽器上一頁可用。

**現有 → 新的對應**：
- 儀表板 → `#dashboard`（重構為 panel 化）
- 主播清單 → `#streamers`（改名「主播管理」）
- 彈幕查詢 → `#danmaku`
- 錄影回放 → `#player`（加右側彈幕欄）
- 儀表板的「⚙ 通知設定」彈出面板 → 拆進 `#settings/*` 五個子頁

---

## 2. 全域框架

```
┌────────────────┬──────────────────────────────────────────────┐
│  ▣ DLR 控制台   │  設定 › 通知              ● 正常 · :8765 · ⟳   │  ← Topbar
├────────────────┼──────────────────────────────────────────────┤
│ 監控            │                                              │
│  📊 儀表板  ●   │                                              │
│  🎬 主播管理    │              主內容區（依 hash 切換）          │
│ 內容            │                                              │
│  💬 彈幕        │                                              │
│  ▶️ 回放        │                                              │
│  📈 統計        │                                              │
│ 設定            │                                              │
│  🎥 錄製        │                                              │
│  🔔 通知  ◀     │                                              │
│  🗜️ 壓縮        │                                              │
│  💾 磁碟        │                                              │
│  🛠️ 系統        │                                              │
├────────────────┤                                              │
│ ●錄製器 ●45GB   │                                              │
└────────────────┴──────────────────────────────────────────────┘
```

- **Sidebar**：寬 220px；分組標題灰色小字；當前項左側 accent 直條 + 高亮底。
- **Topbar**：左＝麵包屑（組 › 頁）；右＝全域健康燈（●綠/黃/紅，hover 顯示問題摘要）、實際 port、手動刷新鈕。
- **Sidebar 底部**：常駐迷你狀態——錄製器活著嗎（●）、磁碟剩餘（GB）。全域可見，解決「切頁看不到系統狀態」。
- **Responsive**：< 900px 時 sidebar 收成抽屜，topbar 左出現漢堡鈕；表格可橫向捲動。

---

## 3. 各頁線框

### 3.1 儀表板 `#dashboard`（panel 化）

一眼掌握全局。四區：KPI 列 → 健康/磁碟 → 正在錄製 → 近期活動。

```
┌── KPI 列（6 卡）──────────────────────────────────────────────┐
│ 監測中 12 │ 錄製中 3 │ 今日 5h20m │ 歷史 320h │ 硬碟用 1.8TB │ 剩餘 45GB │
└──────────────────────────────────────────────────────────────┘
┌── 系統健康（panel）──────────────┬── 磁碟（panel）──────────────┐
│ ● 系統正常                        │  [■■■■■■■■□□] 80% 已用       │
│ ✓ 錄製器執行中                    │  總 2TB · 用 1.8TB · 剩 45GB  │
│ ✓ 資料庫正常                      │  壓縮已省 120GB               │
│ ⚠ 京圈太子 cookie 可能過期        │  [自動管理設定 →]             │
└──────────────────────────────────┴──────────────────────────────┘
┌── 正在錄製（panel，即時）────────────────── [↻ 立即檢查開播] ─┐
│ ● 主播A  抖音  20:04 開始 · 1h12m  ▓▓▓▓▓░ 60/60m  [停止]        │
│ ● 主播B  抖音  21:30 開始 · 24m    ─                [停止]        │
│ ● 主播C  抖音  21:50 開始 · 4m     ─                [停止]        │
└──────────────────────────────────────────────────────────────┘
```
- **「↻ 立即檢查開播」按鈕**：強制觸發 recorder 立即重掃整個開播列表、決定該不該開錄，不用等下一次輪詢週期（一般 300s / 特別關注 60s）。對應現有 `POST /api/recorder/restart`（kill + 重啟 exe → 一啟動就掃全部 URL）。按下時要有確認/loading 態：**進行中的錄製會被短暫中斷**（`taskkill /f /t` 連同 ffmpeg 一起殺掉、幾秒後新行程重新偵測到開播再開一個新檔案），不是原本誤以為的「續錄不中斷」——那其實是修復前孤兒 ffmpeg 沒被清乾淨的副作用（見 AGENT.md §7），已修正為乾淨重啟。主播管理頁工具列也放一顆同功能鈕。
  > ⚠️ **重設計要修正的混淆點**：現狀有**兩顆都用 ↻ 圖示**的按鈕——savebar 的「立即檢查開播」(`/api/recorder/restart`) 與版本區的「檢查更新」(`/api/update/check`)，視覺上難分辨，使用者常誤認。重設計要**明確區分**：本按鈕用「▶ 檢查開播」或「↻ 檢查開播」加**清楚文字標籤**放在「正在錄製」panel 與主播管理工具列；「檢查更新」改放進 `#settings/system` 頁、用不同圖示與文字，不再放頂部裸圖示。
```
┌── 壓縮佇列（panel，有在壓才顯示）──┬── 近期通知（panel）─────────┐
│ 總進度 ▓▓▓░ 32% (3/10)            │ 🔴 20:04 主播A 開始錄製      │
│ · NVIDIA: fileX.mp4  26%          │ ⏹ 19:50 主播D 結束(2h)       │
│ · CPU:    fileY.mp4  71%          │ ⚠ 18:30 磁碟偏低             │
└────────────────────────────────────┴──────────────────────────┘
```

- KPI 卡可點 → 跳對應頁（錄製中 → 主播管理篩選正在錄）。
- 「正在錄製」panel 每 5 秒刷新，進度條顯示距上限剩餘。
- panel 有資料才顯示（壓縮佇列閒置時隱藏），避免空面板佔位。

### 3.2 主播管理 `#streamers`

```
┌ 工具列 ────────────────────────────────────────────────────┐
│ [🔍搜尋 主播/備註/URL]   [＋新增主播] [清空自動恢復] [批量▾]  │
└────────────────────────────────────────────────────────────┘
┌ 表格 ──────────────────────────────────────────────────────┐
│ ☐ 啟用 關注 主播 平台 畫質 直播間 單場 恢復 累積 場次 硬碟 狀態 動作│
│ ☐  ⚫   ★  主播A 抖音 原畫 ...    60分 —  12h  30  8GB ●LIVE 📂✎🗑│
│ ...（可排序、多選）                                            │
└────────────────────────────────────────────────────────────┘
```
- 沿用現有欄位；批量操作收進「批量 ▾」下拉，減少工具列擁擠。
- 狀態徽章：●LIVE（綠）/ 冷卻中（琥珀）/ 監測中 / 停用（灰）。
- 空狀態：「還沒有主播，點『＋新增主播』開始」。

### 3.3 彈幕 `#danmaku`

```
┌ 擷取狀態列 ─ 彈幕擷取：啟用中(連線 3)  [停用擷取]              ┐
┌ 篩選列 ─ [主播▾] [🔍關鍵字] [發言者] [起日] [查詢] [匯出CSV]   ┐
┌ 表格 ─ 時間 | 主播 | 發言者 | 內容 ─────────────────────────┐
│ ...（分頁）                                                    │
└──────────────────────────────────────────────────────────────┘
```
（結構沿用現有，套新元件樣式；「彈幕轉字幕」區塊移到頁首一個 panel。）

### 3.4 回放 `#player`（雙欄 + 同步彈幕）★ 本次重點

```
┌ 日期 ─┬─────── 播放器 ────────────┬──── 彈幕（可收合）─────┐
│ 6-24● │  ┌──────────────────────┐  │ [🔍搜尋彈幕]  [☰收合]   │
│ 6-23  │  │                      │  │ ┌───────────────────┐ │
│ 6-22  │  │      <video>         │  │ │12:03 小明: 你好     │ │
│  ...  │  │                      │  │ │12:05 小華: 666  ◀現在│ │ ← 跟播高亮
│       │  └──────────────────────┘  │ │12:07 阿美: 晚安     │ │
│ 當天  │  ▶ 20:04 主播A (1/3)       │ │ ...(自動捲動)       │ │
│ 清單  │  [⏮][⏭][☑自動下一段]      │ │ └───────────────────┘ │
│ ·段1● │  [☑彈幕字幕(疊在影片上)]    │ │ ☑時間戳 ☑發言者       │
│ ·段2  │                            │ │                       │
└───────┴────────────────────────────┴────────────────────────┘
```

**彈幕同步行為（本次新增，資料靠「彈幕 ts − 錄影 start_time = 影片秒數」）**：
1. **跟播捲動**：影片播到 t 秒，右側自動捲到 t 秒的彈幕並高亮「目前」該行；使用者拖動進度條，彈幕跟著跳。
2. **點彈幕跳轉**：點任一則彈幕 → 影片 `currentTime` 跳到該彈幕時間點。
3. **可開關彈幕欄**：☰ 一鍵收合右欄，讓影片區變大。
4. **顯示選項**：時間戳（影片內 mm:ss，可點）、發言者名稱，各自可開關。
5. **關鍵字搜尋 / 跳轉**：搜這段錄影的彈幕，點結果跳到影片對應時間。
6. 與既有的「彈幕字幕疊在影片上」（VTT track）並存，兩者資料同源。

**技術備註**：
- 右欄彈幕一次載入整段（`/api/danmaku?url=&since=&until=` 已足夠；回放頁需能用 clip 反推 url+時間範圍→ 可能需在 `/api/library/day` 的 clip 物件補上 `live_url`/`start_epoch`/`duration`，或加輕量端點 `/api/library/clip/<id>/danmaku`）。此為實作階段要補的資料需求（見 §6）。
- 跟播用 `<video>` 的 `timeupdate` 事件 + 二分搜尋定位目前彈幕索引，避免每次線性掃描。

### 3.5 統計 `#stats`（P1-2 新）

```
┌ 篩選 ─ [全部歷史▾ / 本月 / 自訂日期]  [匯出CSV]              ┐
┌ 排行榜 panel ──────────────┬ 摘要 panel ───────────────────┐
│ # 主播      時長    場次     │ 總時長 320h · 總場次 1,240     │
│ 1 主播A    45h20m   30      │ 平均每場 15m · 最長 3h12m      │
│ 2 主播B    38h10m   28      │ 今日 5h · 本月 82h             │
│ ...                         │                               │
└─────────────────────────────┴───────────────────────────────┘
┌ 單一主播明細（點排行榜某人展開）───────────────────────────┐
│ 開始 | 結束 | 時長 | 畫質 | 狀態 ...（該主播歷次錄製）        │
└──────────────────────────────────────────────────────────────┘
```
- 資料全來自現有 `query_duration` 的 DB 查詢（`total_by_anchor` / `sessions_for` / `total_seconds`），只是搬進 UI。

### 3.6 設定（5 子頁，共用「儲存列」樣式）

每個子頁**底部固定一條儲存列**：`[儲存] [測試/預覽]  狀態訊息`。危險操作走「預覽 → 確認」兩段式。

**🎥 錄製 `#settings/recording`（P1-1 新，讀寫 config.ini）**
```
畫質 [原畫▾]   保存路徑 [F:\main\record...] [瀏覽]
分資料夾: ☑作者 ☐時間 ☐標題    檔名含標題 ☐
影片格式 [ts▾]   循環時間 [300]秒   分段 ☑ 每[1800]秒
[Cookie 設定 ▾]  抖音 [........]  快手 [........] ...
─────────────────────────────────────────────
[儲存]  ⚠ 部分設定需重啟錄製器生效
```
> ⚠️ config.ini 由 recorder(exe) 讀取，UI 存檔後需 recorder 重啟/下輪才套用 → 存檔後明確提示。

**🔔 通知 `#settings/notify`**：健康警報（ntfy/TG/webhook + 門檻）＋ Discord 錄製通知（webhook/proxy/開始結束/測試/傳送目前狀態）——把現有彈出面板內容搬過來、分兩區塊。

**🗜️ 壓縮 `#settings/compress`**（本次細化）
```
┌ 自動壓縮 ──────────────────────────────────────────────┐
│ ☑ 啟用自動壓縮（背景每 N 分鐘自動掃描並壓縮）           │
│   關閉時完全不自動執行，只能手動觸發 ↓                  │
│ [ ▶ 立即開始壓縮 ]  [ ⏸ 暫停 ]                          │
└────────────────────────────────────────────────────────┘
┌ 壓縮總覽（占比）──────────────────────────────────────┐
│ 全部影片 480 檔 · 1.8TB                                 │
│ 已壓縮   ▓▓▓▓▓▓▓░░░ 68%  (326 檔 · 1.1TB → 省 120GB)   │
│ 未壓縮   32% (154 檔 · 0.7TB 待處理)                    │
└────────────────────────────────────────────────────────┘
┌ 目前壓縮中 ───────────────────────────────────────────┐
│ 總進度 ▓▓▓░ 32% (完成 3/10, 並行 2 線)                  │
│ · NVIDIA 獨顯  一點27_....mp4   26.1%  1m23s            │
│ · CPU         有一點27_....mp4  71.0%  3m02s            │
└────────────────────────────────────────────────────────┘
┌ 參數 ─ 編碼器[多選] 畫質[CRF] 速度[preset] 靜置[分] 最小檔[MB] ┐
[儲存]
```
- **手動觸發**：即使「啟用自動壓縮」關閉，也能按「▶ 立即開始壓縮」跑一輪；開關只控制**是否自動**背景執行。
- **壓縮總覽/占比**（⚠️ 新後端需求，見 §6）：掃保存夾統計 全部/已壓縮(_hevc)/未壓縮 的檔數與大小，算出已壓縮占比與待處理量。
- **目前壓縮中**：沿用現有 per-lane 進度（`/api/compress` 的 `lanes`/`queue`）。
- 編碼器多選、CRF/preset/靜置/最小檔沿用現有，套新樣式。

**💾 磁碟 `#settings/disk`**：暫停門檻、自動清理（預設關、預覽再執行）、保留天數。

**🛠️ 系統 `#settings/system`**：GitHub 更新狀態/檢查/套用、實際 port、log 位置、關於/版本。

---

## 4. Design Tokens

```css
/* 背景三層次 + 表面 */
--bg:        #0b1120;   /* 最深（body） */
--surface:   #151d2e;   /* 卡片/panel */
--surface-2: #1e293b;   /* 表頭/hover/輸入框 */
--border:    #2a3648;   /* 邊框 */
--border-strong: #3a4a63;

/* 文字 */
--fg:      #e6edf6;     /* 主文字 */
--fg-dim:  #9fb0c3;     /* 次要 */
--fg-muted:#647388;     /* 弱化/placeholder */

/* 品牌 + 語意狀態 */
--accent:  #38bdf8;     /* 主強調（青藍） */
--accent-strong:#0ea5e9;
--ok:      #22c55e;     /* 正常/錄製中 */
--warn:    #f59e0b;     /* 注意 */
--danger:  #ef4444;     /* 嚴重/危險操作 */
--info:    #818cf8;

/* 間距（4px 網格） */
--sp-1:4px --sp-2:8px --sp-3:12px --sp-4:16px --sp-5:24px --sp-6:32px

/* 圓角 / 字級 / 陰影 */
--radius-sm:6px --radius:10px --radius-lg:14px
--fs-xs:12px --fs-sm:13px --fs-md:14px --fs-lg:18px --fs-xl:24px
--shadow-1: 0 1px 2px rgba(0,0,0,.3);
--shadow-2: 0 4px 12px rgba(0,0,0,.35);
```
比現狀精緻的關鍵：**三層背景 + 邊框/陰影拉出層次**（目前偏平），字級/間距統一成 token（目前 inline 硬值）。亮/暗主題本次不做（維持暗色），但 token 化後未來要加很容易。

---

## 5. 元件規範

| 元件 | 規格重點 |
|------|----------|
| **Card / Panel** | `--surface` 底、`--border` 框、`--radius`、`--shadow-1`；標題列（左標題右動作）。 |
| **KPI 卡** | 大數字（`--fs-xl`）+ 灰標籤；可點時 hover 邊框變 accent。 |
| **Table** | 表頭 `--surface-2` 黏頂；列 hover 亮一階；可排序欄有箭頭；zebra 可選。 |
| **Form row** | 標籤左、控件右；輸入框 `--surface-2` 底 focus 時 accent 邊框；hint 灰小字。 |
| **Button** | 主(accent 實心)/次(邊框)/危險(紅)；小尺寸給表格內；disabled 降透明。 |
| **Badge/Pill** | 狀態語意色：LIVE(綠)、冷卻(琥珀)、停用(灰)、嚴重(紅)。 |
| **Switch** | 沿用現有 slider，套 token。 |
| **Toast** | 右下角浮出、2.5s 自動消失、成功/錯誤兩色。 |
| **Modal** | 遮罩 + 置中卡；Esc 關閉；點遮罩關閉；危險確認用紅色主鈕。 |
| **空狀態** | 圖示 + 一句說明 + 主要行動鈕。 |
| **載入骨架** | 表格/卡片載入時顯示灰塊動畫，取代「載入中…」純文字。 |
| **健康燈** | 小圓點 ●，綠/琥珀/紅；hover tooltip 列問題。 |
| **並排多欄清單/表格** | 同一列並排的區塊要有共同高度基準（`height:70vh` 等）+ `display:flex; flex-direction:column`；內部清單/表格用 `flex:1; min-height:0; overflow:auto`（不是 `max-height`），資料再多也封頂在該高度、內部出 scrollbar，不撐高整列、不擠壓旁邊區塊。上下對半分的兩塊各給 `flex:1 1 0`。實例：回放頁 `#pl-anchors`／`#pl-dates`／`#pl-clips`／`#pl-dm-col`。 |

---

## 6. 新頁/新功能的資料需求（實作階段要補的後端）

| 需求 | 後端變更 |
|------|----------|
| 錄製設定 GUI（P1-1） | 新增 `GET/PUT /api/config`：讀寫 config.ini 白名單鍵（畫質/路徑/分資料夾/格式/循環/分段/cookie…）。用 configparser 或既有的正則讀寫，保留註解與中文鍵。存檔後回傳「需重啟 recorder」提示旗標。 |
| 統計頁（P1-2） | 多為現有查詢；可加 `GET /api/stats/top?since=`、`GET /api/stats/anchor?name=`（包既有 duration_tracker 查詢）。 |
| 回放同步彈幕 | `/api/library/day` 的 clip 物件補 `live_url` + `start_epoch` + `duration`；或加 `GET /api/library/clip/<id>/danmaku` 直接回該片段時間範圍內的彈幕（附影片內秒數偏移）。 |
| 壓縮占比總覽 | 新增 `GET /api/compress/overview`：掃 `get_save_path()` 下影片，統計 全部/已壓縮(_hevc 結尾)/未壓縮 的檔數與位元組，回 `{total, compressed, pending, compressed_ratio, saved_bytes}`。掃描結果快取 60s（沿用 `get_folder_size_cached` 的快取模式，別每次重掃）。 |
| 全域健康列/迷你狀態 | 沿用現有 `/api/health`、`/api/status`；前端全域輪詢一次共用。 |

> 原則不變：不加第三方依賴、純邏輯與 IO 分離、破壞性操作預設關+可預覽、存檔用 JSON deep-merge（config.ini 除外，要保留原格式）。

---

## 7. 分階段實作計畫（每階段可獨立驗證、不破壞現有功能）

**階段 0：骨架**（不動功能）
- 建 design tokens CSS、元件 class、Alpine 本地檔進 `web_static/`。
- 建側邊欄 + topbar + hash 路由的空殼，5+ 個 view 容器。

**階段 1：遷移現有頁**（等價替換，先不加新功能）
- 儀表板（先搬，再 panel 化）、主播管理、彈幕、回放搬進新框架，套新元件。
- 「⚙ 通知設定」彈出面板內容拆進 `#settings/notify · compress · disk · system` 四子頁。
- 驗證：所有現有 API 呼叫與互動行為不變。

**階段 2：儀表板 panel 化 + 全域狀態**
- 儀表板改多 panel 佈局；topbar 健康燈 + sidebar 底迷你狀態接上。

**階段 3：回放同步彈幕**（新功能）
- 補後端 clip 彈幕資料；右側彈幕欄 + 跟播捲動 + 點擊跳轉 + 搜尋 + 開關 + 時間戳/發言者。

**階段 4：新頁**
- `#settings/recording`（P1-1，含 `/api/config`）
- `#stats`（P1-2）

**階段 5：技術債清理**（見 §9；穿插在各階段做更好）
- 抽 `src/common.py`（deep_merge / json settings / http_post_json / fmt_duration），改各呼叫端。
- 移除死碼（_now_hm、單則 embed/text、孤兒 diff_issue_codes、未用 import）。
- widget 標記 deprecated。

每階段結束：`py_compile` 後端、HTML/JS 一致性檢查、手動點過主要流程；文件同步（README 教學 + ROADMAP 標完成 + PROJECT_STATUS 補條）。

---

## 9. 技術債清理（趁重構一併處理）

實作前已掃過現有碼，確認的**重複 / 死碼 / 過期**清單：

### 9.1 重複程式碼 → 抽共用模組
建議新增 `src/common.py`（純標準庫、無依賴），把以下重複實作集中：

| 重複函式 | 目前散落 | 處理 |
|----------|----------|------|
| `_deep_merge(base, override)` | `alerts.py`、`compressor.py`、`rec_notify.py` 三份一模一樣 | 抽到 `common.deep_merge`，三處改 import |
| JSON 設定 `load_settings`/`save_settings`（deep-merge 模式） | `compressor.py`、`rec_notify.py`（`alerts.py` 也類似） | 抽 `common.load_json_settings(path, defaults)` / `save_json_settings(path, data, defaults)` |
| `_post_json` / webhook POST（`build_opener` + UA + 錯誤處理） | `alerts.py`、`rec_notify.py`（`room.py` 是上游、不動） | 抽 `common.http_post_json(url, payload, proxy, ua)`，回 `(ok, detail)` |
| 時長格式化 `_fmt_dur` / `timedelta` 秒→h:m:s | `danmaku_subtitle.py`、`duration_tracker.py`、`rec_notify.py`、`query_duration.py` | 抽 `common.fmt_duration(sec)`（統一輸出格式） |
| 資料夾大小/掃描快取（`get_folder_size_cached` 模式） | `web_ui.py` | 壓縮占比總覽（§6）沿用同一份，別再寫一套 |

> 原則：抽共用**不改行為**、逐一改完各呼叫端後跑既有測試確認等價；上游檔案（`spider/stream/room/utils/ab_sign/proxy/logger/initializer`）**不碰**，只動自訂層。

### 9.2 死碼 / 未使用 → 移除
- `rec_notify.py: _now_hm()` — 加了但沒用到（時間戳走 embed `timestamp`），移除。
- `rec_notify.py: format_start()/format_stop()`（單則純文字）— 改批次合併卡片後，主流程已改用 `format_batch_text` / `build_batch_embed`；確認無其他呼叫端後移除（或保留給通用 webhook fallback，二擇一，不要兩套並存）。
- `rec_notify.py: build_start_embed()/build_stop_embed()`（單則 embed）— 同上，批次版取代；確認後移除。
- `health.py: diff_issue_codes()` — 目前無呼叫端；若重設計的健康列不需要就移除，需要就接上，不要留著孤兒函式。
- 各檔開頭未使用的 import（實作時用 `py_compile` + 檢視逐一清）。

### 9.3 過期 / 被取代
- `widget.py` / `widget_window.py` / `web_static/widget.html`（托盤 + 浮動視窗）：widget 在此機器會無聲退出、`start_console.bat` 已跳過、瀏覽器控制台已完全取代其功能。**建議標記為 deprecated**（README 註明、autostart 不裝），本次重設計**不為 widget 做新 UI**。是否實際刪檔由你決定（見下方確認）。
- `start_widget.bat`：功能被 `start_console.bat` 取代；保留但 README 標註建議用後者。

### 9.4 清理做法（安全）
- 每項清理**獨立小步 + 跑測試**，不與 UI 改動混在同一步，方便出錯時定位。
- 移除前先 `grep` 全專案確認零呼叫端（含 `web_ui.py`、CLI、web_static 的 JS）。
- 抽共用後，原本各功能的單元測試改指向 `common.*` 重跑，確保等價。

---

## 10. 明確不做（本次）
- 亮色主題（token 已備好，未來再加）。
- 多使用者/權限、雲端同步（違背個人自用定位）。
- 前端建置工具鏈（維持零建置）。
- 重寫後端 API（以沿用 + 少量新增為主）。
